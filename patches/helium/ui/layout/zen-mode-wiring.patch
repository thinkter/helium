--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -188,6 +188,12 @@
       <message name="IDS_SETTINGS_ACCESSIBILITY_TOAST_FREQUENCY_DESCRIPTION" desc="Subtitle for a setting that allows you to change the frequency of feedback messages.">
         Shows a confirmation when you copy links, images, or videos
       </message>
+      <message name="IDS_SETTINGS_ACCESSIBILITY_TOAST_TOGGLE_TITLE" desc="Title for a setting that allows you to toggle clipboard copy and new tab toast notifications.">
+        Background action confirmation toasts
+      </message>
+      <message name="IDS_SETTINGS_ACCESSIBILITY_TOAST_TOGGLE_DESCRIPTION" desc="Subtitle for a setting that allows you to toggle clipboard copy and new tab toast notifications.">
+        Shows a toast notification when you copy content, such as links and images, or open a new background tab in Frameless mode
+      </message>
     </if>
 
     <!-- Appearance Page -->
@@ -295,6 +301,18 @@
     <message name="IDS_SETTINGS_MINIMAL_LOCATION_BAR" desc="Label for the toggle that enables the minimal URL in the address bar, showing only the site origin.">
       Minimal URL in the address bar
     </message>
+    <message name="IDS_SETTINGS_BROWSER_ZEN_MODE" desc="Label for the toggle that enables zen mode, hiding browser chrome until hover.">
+      Frameless mode
+    </message>
+    <message name="IDS_SETTINGS_BROWSER_ZEN_MODE_DESCRIPTION" desc="Description for the zen mode toggle in appearance settings.">
+      Automatically hide browser UI until you hover the window edge
+    </message>
+    <message name="IDS_SETTINGS_ZEN_PIN_SIDEBAR" desc="Description for the toggle to pin the sidebar (vertical tab strip) when in Zen mode.">
+      Always show the sidebar
+    </message>
+    <message name="IDS_SETTINGS_ZEN_PIN_TOP_TOOLBAR" desc="Description for the toggle to pin the top bar when in Zen mode.">
+      Always show the top bar
+    </message>
     <message name="IDS_SETTINGS_SHOW_HOME_BUTTON" desc="Label for the checkbox which enables or disables showing the home button in the toolbar.">
       Show home button
     </message>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -286,9 +286,9 @@ void AddA11yStrings(content::WebUIDataSo
       {"focusHighlightLabel",
        IDS_SETTINGS_ACCESSIBILITY_FOCUS_HIGHLIGHT_DESCRIPTION},
       {"toastAlertLevelTitle",
-       IDS_SETTINGS_ACCESSIBILITY_TOAST_FREQUENCY_TITLE},
+       IDS_SETTINGS_ACCESSIBILITY_TOAST_TOGGLE_TITLE},
       {"toastAlertLevelDescription",
-       IDS_SETTINGS_ACCESSIBILITY_TOAST_FREQUENCY_DESCRIPTION},
+       IDS_SETTINGS_ACCESSIBILITY_TOAST_TOGGLE_DESCRIPTION},
 #endif
 #if BUILDFLAG(IS_WIN) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_MAC)
       {"overscrollHistoryNavigationTitle",
@@ -604,6 +604,10 @@ void AddAppearanceStrings(content::WebUI
       {"browserLayoutCompact", IDS_SETTINGS_BROWSER_LAYOUT_COMPACT},
       {"browserLayoutVertical", IDS_SETTINGS_BROWSER_LAYOUT_VERTICAL},
       {"tabStripRightAlign", IDS_SETTINGS_TAB_STRIP_RIGHT_ALIGN},
+      {"browserZenMode", IDS_SETTINGS_BROWSER_ZEN_MODE},
+      {"browserZenModeDescription", IDS_SETTINGS_BROWSER_ZEN_MODE_DESCRIPTION},
+      {"zenModePinSidebar", IDS_SETTINGS_ZEN_PIN_SIDEBAR},
+      {"zenModePinToolbar", IDS_SETTINGS_ZEN_PIN_TOP_TOOLBAR},
       {"centeredLocationBar", IDS_SETTINGS_CENTERED_LOCATION_BAR},
       {"verticalCollapseShortcut", IDS_SETTINGS_VERTICAL_COLLAPSE_SHORTCUT},
       {"minimalLocationBar", IDS_SETTINGS_MINIMAL_LOCATION_BAR},
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -226,6 +226,11 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHeliumMinimalLocationBar] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kHeliumZenMode] = settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kHeliumZenModeSidebarPinned] =
+      settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kHeliumZenModeTopChromePinned] =
+      settings_api::PrefType::kBoolean;
 
   // Miscellaneous
   (*s_allowlist)[::embedder_support::kAlternateErrorPagesEnabled] =
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.html.ts
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.html.ts
@@ -89,6 +89,23 @@ export function getHtml(this: SettingsAp
           label="$i18n{minimalLocationBar}">
       </settings-toggle-button>
     </div>
+    <settings-toggle-button class="hr"
+        pref-key="helium.browser.zen_mode"
+        label="$i18n{browserZenMode}"
+        sub-label="$i18n{browserZenModeDescription}">
+    </settings-toggle-button>
+    <div class="list-frame indented-toggles"
+        ?hidden="${!this.zenModePref_?.value}">
+      <settings-toggle-button class="hr"
+          pref-key="helium.browser.zen_mode_sidebar_pinned"
+          ?hidden="${!this.showVerticalSettings_()}"
+          label="$i18n{zenModePinSidebar}">
+      </settings-toggle-button>
+      <settings-toggle-button class="hr"
+          pref-key="helium.browser.zen_mode_top_chrome_pinned"
+          label="$i18n{zenModePinToolbar}">
+      </settings-toggle-button>
+    </div>
     <div class="hr" ?hidden="${!this.showHr_(
         this.pageVisibility_.setTheme, this.pageVisibility_.homeButton)}">
     </div>
--- a/chrome/browser/resources/settings/appearance_page/appearance_page.ts
+++ b/chrome/browser/resources/settings/appearance_page/appearance_page.ts
@@ -153,6 +153,7 @@ export class SettingsAppearancePageEleme
       sidePanelIsRightAlignedPref_: {type: Object},
       themeIdPref_: {type: Object},
       themePolicyColorPref_: {type: Object},
+      zenModePref_: {type: Object},
 
       // <if expr="is_linux">
       themeSystemThemePref_: {type: Object},
@@ -220,6 +221,7 @@ export class SettingsAppearancePageEleme
   protected accessor themeIdPref_: PrefObject<string>|undefined = undefined;
   protected accessor themePolicyColorPref_: PrefObject<number>|undefined =
       undefined;
+  protected accessor zenModePref_: PrefObject<boolean>|undefined = undefined;
 
   // <if expr="is_linux">
   protected accessor themeSystemThemePref_: PrefObject<number>|undefined =
@@ -362,6 +364,7 @@ export class SettingsAppearancePageEleme
       'side_panel.alignment_overrides': 'sidePanelAlignmentOverridesPref_',
       'side_panel.is_right_aligned': 'sidePanelIsRightAlignedPref_',
       'toolbar.pinned_actions': 'pinnedActionsPref_',
+      'helium.browser.zen_mode': 'zenModePref_',
       'webkit.webprefs.default_font_size': 'defaultFontSizePref_',
     });
 
--- a/chrome/browser/ui/helium/helium_layout_state_controller.cc
+++ b/chrome/browser/ui/helium/helium_layout_state_controller.cc
@@ -19,8 +19,8 @@ HeliumLayoutStateController::HeliumLayou
                                 *this) {
   pref_change_registrar_.Init(pref_service_);
 
-  pref_change_registrar_.Add(
-      prefs::kHeliumLayout,
+  pref_change_registrar_.AddMultiple(
+      {prefs::kHeliumLayout, prefs::kHeliumZenMode},
       base::BindRepeating(&HeliumLayoutStateController::NotifyStateChanged,
                           base::Unretained(this)));
 }
@@ -49,6 +49,10 @@ bool HeliumLayoutStateController::IsDyna
   return GetBrowserLayout() == HeliumLayoutType::kDynamic;
 }
 
+bool HeliumLayoutStateController::IsZenModeEnabled() const {
+  return pref_service_->GetBoolean(prefs::kHeliumZenMode);
+}
+
 void HeliumLayoutStateController::SetBrowserLayout(HeliumLayoutType layout) {
   pref_service_->SetInteger(prefs::kHeliumLayout, std::to_underlying(layout));
 }
--- a/chrome/browser/ui/helium/helium_layout_state_controller.h
+++ b/chrome/browser/ui/helium/helium_layout_state_controller.h
@@ -39,6 +39,7 @@ class HeliumLayoutStateController {
   bool IsCompactLayout() const;
   bool IsVerticalLayout() const;
   bool IsDynamicLayout() const;
+  bool IsZenModeEnabled() const;
 
   void SetBrowserLayout(HeliumLayoutType layout);
   HeliumLayoutType GetBrowserLayout() const;
--- a/chrome/browser/ui/tabs/tab_strip_prefs.cc
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.cc
@@ -34,6 +34,9 @@ void RegisterProfilePrefs(user_prefs::Pr
   registry->RegisterIntegerPref(prefs::kHeliumLayout,
                                 std::to_underlying(HeliumLayoutType::kClassic));
   registry->RegisterBooleanPref(prefs::kHeliumVerticalRightAligned, false);
+  registry->RegisterBooleanPref(prefs::kHeliumZenMode, false);
+  registry->RegisterBooleanPref(prefs::kHeliumZenModeSidebarPinned, false);
+  registry->RegisterBooleanPref(prefs::kHeliumZenModeTopChromePinned, false);
 
   registry->RegisterBooleanPref(prefs::kVerticalTabsCollapsedState, false);
   registry->RegisterIntegerPref(prefs::kVerticalTabsUncollapsedWidth,
--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -624,6 +624,17 @@ inline constexpr char kHeliumLayout[] =
 inline constexpr char kHeliumVerticalRightAligned[] =
     "helium.browser.vertical_right_aligned";
 
+// A boolean pref set to true if zen mode is enabled.
+inline constexpr char kHeliumZenMode[] = "helium.browser.zen_mode";
+
+// A boolean pref set to true if the VTS is pinned (non-floating) in zen mode.
+inline constexpr char kHeliumZenModeSidebarPinned[] =
+    "helium.browser.zen_mode_sidebar_pinned";
+
+// A boolean pref set to true if the top toolbar is pinned in zen mode.
+inline constexpr char kHeliumZenModeTopChromePinned[] =
+    "helium.browser.zen_mode_top_chrome_pinned";
+
 // A boolean pref set to true if the address bar should be centered.
 // When false, the address bar uses the full available width.
 inline constexpr char kHeliumCenteredLocationBar[] =
--- a/chrome/browser/ui/views/frame/browser_native_widget_mac.mm
+++ b/chrome/browser/ui/views/frame/browser_native_widget_mac.mm
@@ -440,6 +440,7 @@ void BrowserNativeWidgetMac::ValidateUse
     case IDC_PRINT:
     case IDC_RELOAD:
     case IDC_CTRL_S_SHORTCUT:
+    case IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN:
     case IDC_SAVE_PAGE:
     case IDC_SELECT_NEXT_TAB:
     case IDC_SELECT_PREVIOUS_TAB:
--- a/chrome/app/chrome_command_ids.h
+++ b/chrome/app/chrome_command_ids.h
@@ -120,6 +120,7 @@
 #define IDC_BROWSER_LAYOUT_DYNAMIC      34086
 
 #define IDC_CTRL_S_SHORTCUT             34090
+#define IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN 34091
 
 // Tab group Commands
 #define IDC_ADD_NEW_TAB_TO_GROUP      34100
--- a/chrome/app/chrome_dll.rc
+++ b/chrome/app/chrome_dll.rc
@@ -61,6 +61,7 @@ BEGIN
     "A",            IDC_FOCUS_INACTIVE_POPUP_FOR_ACCESSIBILITY,         VIRTKEY, SHIFT, ALT
     "D",            IDC_FOCUS_LOCATION,         VIRTKEY, ALT
     "L",            IDC_FOCUS_LOCATION,         VIRTKEY, CONTROL
+    "L",            IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN, VIRTKEY, CONTROL, SHIFT
     VK_F10,         IDC_FOCUS_MENU_BAR,         VIRTKEY
     VK_MENU,        IDC_FOCUS_MENU_BAR,         VIRTKEY
     VK_F6,          IDC_FOCUS_NEXT_PANE,        VIRTKEY
--- a/chrome/browser/global_keyboard_shortcuts_mac.mm
+++ b/chrome/browser/global_keyboard_shortcuts_mac.mm
@@ -144,6 +144,7 @@ const std::vector<KeyboardShortcutData>&
       {true,  false, false, false, kVK_ANSI_Keypad9,      IDC_SELECT_LAST_TAB},
 
       {true,  false, false, false, kVK_ANSI_S,            IDC_CTRL_S_SHORTCUT},
+      {true,  true,  false, false, kVK_ANSI_L,            IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN},
 
       {true,  true,  false, false, kVK_ANSI_M,            IDC_SHOW_AVATAR_MENU},
       {true,  false, false, true,  kVK_ANSI_L,            IDC_SHOW_DOWNLOADS},
--- a/chrome/browser/ui/accelerator_table.cc
+++ b/chrome/browser/ui/accelerator_table.cc
@@ -69,6 +69,8 @@ const AcceleratorMapping kAcceleratorMap
     {ui::VKEY_G, ui::EF_SHIFT_DOWN | ui::EF_PLATFORM_ACCELERATOR,
      IDC_FIND_PREVIOUS},
     {ui::VKEY_L, ui::EF_PLATFORM_ACCELERATOR, IDC_FOCUS_LOCATION},
+    {ui::VKEY_L, ui::EF_SHIFT_DOWN | ui::EF_PLATFORM_ACCELERATOR,
+     IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN},
     {ui::VKEY_O, ui::EF_PLATFORM_ACCELERATOR, IDC_OPEN_FILE},
     {ui::VKEY_P, ui::EF_PLATFORM_ACCELERATOR, IDC_PRINT},
     {ui::VKEY_R, ui::EF_PLATFORM_ACCELERATOR, IDC_RELOAD},
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -2056,6 +2056,8 @@ void BrowserCommandController::InitComma
   // These are always enabled; the menu determines their menu item visibility.
   command_updater_->UpdateCommandEnabled(IDC_UPGRADE_DIALOG, true);
   command_updater_->UpdateCommandEnabled(IDC_HELIUM_SERVICES_OPEN, true);
+  command_updater_->UpdateCommandEnabled(IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN,
+                                         false);
   command_updater_->UpdateCommandEnabled(IDC_SET_BROWSER_AS_DEFAULT, true);
 
   // Safety Hub commands.
--- a/chrome/browser/ui/cocoa/accelerators_cocoa.mm
+++ b/chrome/browser/ui/cocoa/accelerators_cocoa.mm
@@ -49,6 +49,8 @@ const struct AcceleratorMapping {
     {IDC_PRINT, ui::EF_COMMAND_DOWN, ui::VKEY_P},
     {IDC_RESTORE_TAB, ui::EF_COMMAND_DOWN | ui::EF_SHIFT_DOWN, ui::VKEY_T},
     {IDC_CTRL_S_SHORTCUT, ui::EF_COMMAND_DOWN, ui::VKEY_S},
+    {IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN,
+     ui::EF_COMMAND_DOWN | ui::EF_SHIFT_DOWN, ui::VKEY_L},
     {IDC_SHOW_BOOKMARK_BAR, ui::EF_COMMAND_DOWN | ui::EF_SHIFT_DOWN,
      ui::VKEY_B},
     {IDC_SHOW_BOOKMARK_MANAGER, ui::EF_COMMAND_DOWN | ui::EF_ALT_DOWN,
--- a/chrome/app/generated_resources.grd
+++ b/chrome/app/generated_resources.grd
@@ -19488,6 +19488,9 @@ Please help our engineers fix this probl
       <message name="IDS_LINK_COPIED_TOAST_BODY" desc="Text on a toast notification that is shown when a link is successfully copied.">
         Link copied
       </message>
+      <message name="IDS_ZEN_MODE_NEW_TAB_CREATED_TOAST_BODY" desc="Text on a toast notification that is shown when a link opens a new tab while Zen Mode is enabled.">
+        New tab opened
+      </message>
       <message name="IDS_IMAGE_COPIED_TOAST_BODY" desc="Text on a toast notification that is shown when an image is successfully copied.">
         Image copied
       </message>
--- a/chrome/browser/ui/toasts/api/toast_id.cc
+++ b/chrome/browser/ui/toasts/api/toast_id.cc
@@ -73,6 +73,8 @@ std::string_view GetToastName(ToastId to
       return "MultistepFilterSuggestionRecent";
     case ToastId::kSkillSavedWithoutInvokeButton:
       return "SkillSavedWithoutInvokeButton";
+    case ToastId::kZenModeNewTabCreated:
+      return "ZenModeNewTabCreated";
     case ToastId::kSendTabToSelfTabOpened:
       return "SendTabToSelfOpened";
     case ToastId::kSendTabToSelfTabsOpenedInBackground:
--- a/chrome/browser/ui/toasts/api/toast_id.h
+++ b/chrome/browser/ui/toasts/api/toast_id.h
@@ -76,7 +76,8 @@ enum class ToastId {
   kIndigoDeleteError = 53,
   kIndigoDeleteSuccess = 54,
   kAtMemorySpiiFetchErrorMessage = 55,
-  kMaxValue = kAtMemorySpiiFetchErrorMessage,
+  kZenModeNewTabCreated = 56,
+  kMaxValue = kZenModeNewTabCreated,
 };
 // LINT.ThenChange(/tools/metrics/histograms/metadata/toasts/enums.xml:ToastId)
 
--- a/tools/metrics/histograms/metadata/toasts/enums.xml
+++ b/tools/metrics/histograms/metadata/toasts/enums.xml
@@ -115,6 +115,7 @@ chromium-metrics-reviews@google.com.
   <int value="53" label="Indigo delete uploaded photo error"/>
   <int value="54" label="Indigo delete uploaded photo success"/>
   <int value="55" label="Showed AtMemory SPII fetch error message"/>
+  <int value="56" label="New tab created in Zen mode"/>
 </enum>
 
 <!-- LINT.ThenChange(/chrome/browser/ui/toasts/api/toast_id.h:ToastId) -->
