--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
@@ -19,6 +19,7 @@
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
 #include "chrome/browser/ui/color/chrome_color_id.h"
 #include "chrome/browser/ui/frame/window_frame_util.h"
+#include "chrome/browser/ui/helium/helium_layout_state_controller.h"
 #include "chrome/browser/ui/immersive/immersive_mode_controller.h"
 #include "chrome/browser/ui/layout_constants.h"
 #include "chrome/browser/ui/tabs/features.h"
@@ -83,7 +84,8 @@ class FrameGrabHandle : public views::Vi
   METADATA_HEADER(FrameGrabHandle, views::View)
 
  public:
-  FrameGrabHandle() {
+  explicit FrameGrabHandle(BrowserWindowInterface* browser)
+      : browser_(browser) {
     SetProperty(views::kElementIdentifierKey,
                 kTabStripFrameGrabHandleElementId);
   }
@@ -105,16 +107,22 @@ class FrameGrabHandle : public views::Vi
 
   gfx::Size CalculatePreferredSize(
       const views::SizeBounds& available_size) const override {
-    // Reserve some space for the frame to be grabbed by, even if the tabstrip
-    // is full.
-    // TODO(tbergquist): Define this relative to the NTB insets again.
-    return gfx::Size(42, 0);
+    // Show the grab handle only in Classic layout
+    if (browser_) {
+      auto* controller = HeliumLayoutStateController::From(browser_);
+      if (controller && controller->IsClassicLayout()) {
+        return gfx::Size(27, 0);
+      }
+    }
+
+    return gfx::Size(0, 0);
   }
 
  private:
   // Anchor point for help bubbles and other dialogs that lies in an empty
   // region of the tabstrip.
   std::unique_ptr<views::ViewSubregionAnchor> dialog_anchor_;
+  raw_ptr<BrowserWindowInterface> browser_ = nullptr;
 };
 
 BEGIN_METADATA(FrameGrabHandle)
@@ -254,7 +262,8 @@ HorizontalTabStripRegionViewOld::Horizon
   }
 
   reserved_grab_handle_space_ =
-      AddChildView(std::make_unique<FrameGrabHandle>());
+      AddChildView(std::make_unique<FrameGrabHandle>(
+          tab_strip_->GetBrowserWindowInterface()));
   reserved_grab_handle_space_->SetProperty(
       views::kFlexBehaviorKey,
       views::FlexSpecification(views::MinimumFlexSizeRule::kPreferred,
@@ -804,7 +813,7 @@ HorizontalTabStripRegionViewNew::Horizon
   }
 
   reserved_grab_handle_space_ =
-      AddChildView(std::make_unique<FrameGrabHandle>());
+      AddChildView(std::make_unique<FrameGrabHandle>(browser));
   reserved_grab_handle_space_->SetProperty(
       views::kFlexBehaviorKey,
       views::FlexSpecification(views::MinimumFlexSizeRule::kPreferred,
