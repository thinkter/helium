--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -136,6 +136,7 @@
 #include "chrome/browser/ui/tabs/tab_utils.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_metrics.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
+#include "chrome/browser/ui/toasts/toast_view.h"
 #include "chrome/browser/ui/toolbar/app_menu_model.h"
 #include "chrome/browser/ui/toolbar/chrome_labs/chrome_labs_utils.h"
 #include "chrome/browser/ui/toolbar/pinned_toolbar/tab_search_toolbar_button_controller.h"
@@ -334,6 +335,7 @@
 #include "ui/events/event_handler.h"
 #include "ui/events/event_utils.h"
 #include "ui/events/keycodes/keyboard_codes.h"
+#include "ui/gfx/animation/animation.h"
 #include "ui/gfx/animation/animation_runner.h"
 #include "ui/gfx/canvas.h"
 #include "ui/gfx/color_utils.h"
@@ -425,6 +427,14 @@ namespace {
 // The visible height of the shadow above the tabs. Clicks in this area are
 // treated as clicks to the frame, rather than clicks to the tab.
 const int kTabShadowSize = 2;
+constexpr int kZenModeRevealTriggerThickness = 6;
+constexpr int kZenModeChromeHoverLeeway = 8;
+constexpr base::TimeDelta kZenModeRevealAnimationDuration =
+    base::Milliseconds(200);
+constexpr base::TimeDelta kZenModeCursorExitGracePeriod =
+    base::Milliseconds(3000);
+constexpr base::TimeDelta kZenModeHoverExitGracePeriod =
+    base::Milliseconds(150);
 
 #if BUILDFLAG(IS_CHROMEOS)
 // UMA histograms that record animation smoothness for tab loading animation.
@@ -970,7 +980,9 @@ BrowserView::BrowserView(Browser* browse
     : views::ClientView(nullptr, nullptr),
       exclusive_access_context_(
           std::make_unique<ExclusiveAccessContextImpl>(*this)),
-      browser_(browser) {
+      browser_(browser),
+      zen_top_chrome_animation_(this),
+      zen_side_chrome_animation_(this) {
   if (auto* manager = InitialWebUIWindowMetricsManager::From(browser_.get())) {
     manager->OnBrowserWindowCreated();
   }
@@ -1001,6 +1013,14 @@ BrowserView::BrowserView(Browser* browse
   }
 
   SetProperty(views::kElementIdentifierKey, kBrowserViewElementId);
+  zen_top_chrome_animation_.SetSlideDuration(
+      gfx::Animation::RichAnimationDuration(kZenModeRevealAnimationDuration));
+  zen_top_chrome_animation_.SetTweenType(gfx::Tween::FAST_OUT_SLOW_IN_3);
+  zen_top_chrome_animation_.Reset(1);
+  zen_side_chrome_animation_.SetSlideDuration(
+      gfx::Animation::RichAnimationDuration(kZenModeRevealAnimationDuration));
+  zen_side_chrome_animation_.SetTweenType(gfx::Tween::FAST_OUT_SLOW_IN_3);
+  zen_side_chrome_animation_.Reset(1);
 
   browser_->tab_strip_model()->AddObserver(this);
 
@@ -1107,6 +1127,14 @@ BrowserView::BrowserView(Browser* browse
       prefs::kFullscreenAllowed,
       base::BindRepeating(&BrowserView::UpdateFullscreenAllowedFromPolicy,
                           base::Unretained(this), CanFullscreen()));
+  registrar_.Add(
+      prefs::kHeliumZenModeSidebarPinned,
+      base::BindRepeating(&BrowserView::OnZenModeSideChromePinnedPrefChanged,
+                          base::Unretained(this)));
+  registrar_.Add(
+      prefs::kHeliumZenModeTopChromePinned,
+      base::BindRepeating(&BrowserView::OnZenModeTopChromePinnedPrefChanged,
+                          base::Unretained(this)));
   UpdateFullscreenAllowedFromPolicy(CanFullscreen());
 
   WebUIContentsPreloadManager::GetInstance()->WarmupForBrowser(browser_.get());
@@ -1184,6 +1212,8 @@ BrowserView::~BrowserView() {
   // Stop the animation timer explicitly here to avoid running it in a nested
   // message loop, which may run by Browser destructor.
   loading_animation_timer_.Stop();
+  zen_mode_event_monitor_.reset();
+  app_menu_control_observation_.Reset();
 
   // Reset autofill bubble handler to make sure it does not out-live toolbar,
   // since it is responsible for showing autofill related bubbles from toolbar's
@@ -1407,6 +1437,13 @@ bool BrowserView::GetTabStripVisible() c
     return false;
   }
 
+  // In zen mode the tab strip is always present, just scaled down.
+  // Returning true keeps the frame's non-client area stable (prevents
+  // NonClientExtraTopThickness from adding extra border pixels).
+  if (IsZenModeEnabled()) {
+    return true;
+  }
+
   // In non-fullscreen the tabstrip should always be visible.
   auto* const immersive_mode_controller =
       ImmersiveModeController::From(browser());
@@ -1619,6 +1656,56 @@ float BrowserView::GetTopControlsSlideBe
   return 1.f;
 }
 
+bool BrowserView::IsZenModeEnabledForLayout() const {
+  return IsZenModeEnabled();
+}
+
+float BrowserView::GetTopChromeRevealRatio() const {
+  if (!zen_mode_enabled_) {
+    return 1.f;
+  }
+  return zen_top_chrome_animation_.GetCurrentValue();
+}
+
+float BrowserView::GetSideChromeRevealRatio() const {
+  if (!zen_mode_enabled_) {
+    return 1.f;
+  }
+  if (!ShouldDrawVerticalTabStrip()) {
+    return 0.f;
+  }
+  return zen_side_chrome_animation_.GetCurrentValue();
+}
+
+bool BrowserView::IsZenModeSideChromePinned() const {
+  return zen_mode_enabled_ && browser_->GetProfile()->GetPrefs()->GetBoolean(
+                                  prefs::kHeliumZenModeSidebarPinned);
+}
+
+bool BrowserView::IsZenModeTopChromePinned() const {
+  return zen_mode_enabled_ && browser_->GetProfile()->GetPrefs()->GetBoolean(
+                                  prefs::kHeliumZenModeTopChromePinned);
+}
+
+void BrowserView::ToggleZenModeSideChrome() {
+  if (!IsZenModeEnabled() || !ShouldDrawVerticalTabStrip()) {
+    return;
+  }
+  const bool was_pinned = IsZenModeSideChromePinned();
+  browser_->GetProfile()->GetPrefs()->SetBoolean(
+      prefs::kHeliumZenModeSidebarPinned, !was_pinned);
+}
+
+void BrowserView::ToggleZenModeTopChrome() {
+  if (!IsZenModeEnabled()) {
+    return;
+  }
+
+  const bool was_pinned = IsZenModeTopChromePinned();
+  browser_->GetProfile()->GetPrefs()->SetBoolean(
+      prefs::kHeliumZenModeTopChromePinned, !was_pinned);
+}
+
 views::Widget* BrowserView::GetWidgetForAnchoring() {
 #if BUILDFLAG(IS_MAC)
   if (WindowFeatureController::From(browser())->UsesImmersiveFullscreenMode()) {
@@ -1655,6 +1742,7 @@ void BrowserView::OnVerticalTabStripMode
   const bool should_be_vertical = controller->ShouldDisplayVerticalTabs();
   if (vertical_initialized == should_be_vertical) {
     // Mode is unchanged; just re-layout so alignment-dependent views update.
+    UpdateZenCollapseButtonVisibility();
     vertical_tab_strip_region_view_->InvalidateLayout();
     InvalidateLayout();
     return;
@@ -1663,6 +1751,7 @@ void BrowserView::OnVerticalTabStripMode
   if (should_be_vertical) {
     horizontal_tab_strip_region_view_->ResetTabStrip();
     vertical_tab_strip_region_view_->InitializeTabStrip();
+    MaybeExpandVerticalTabStripForZenMode();
   } else {
     vertical_tab_strip_region_view_->ResetTabStrip();
     horizontal_tab_strip_region_view_->InitializeTabStrip();
@@ -1699,10 +1788,435 @@ void BrowserView::OnToolbarTabStripState
     toolbar_->UpdateToolbarLayout();
   }
 
+  UpdateZenModeState();
   UpdateTabSearchBubbleHost();
   InvalidateLayout();
 }
 
+void BrowserView::OnZenModeSideChromePinnedPrefChanged() {
+  if (!zen_mode_enabled_) {
+    return;
+  }
+
+  if (IsZenModeSideChromePinned()) {
+    zen_side_chrome_animation_.Reset(1);
+  } else {
+    MaybeExpandVerticalTabStripForZenMode();
+    UpdateZenModeRevealFromCursor();
+  }
+
+  if (vertical_tab_strip_region_view_) {
+    vertical_tab_strip_region_view_->UpdateInteriorMargin();
+  }
+
+  if (multi_contents_view_) {
+    multi_contents_view_->MaybeUpdateContentsBorderAndOverlay();
+  }
+  UpdateZenCollapseButtonVisibility();
+  InvalidateLayout();
+}
+
+void BrowserView::OnZenModeTopChromePinnedPrefChanged() {
+  if (!zen_mode_enabled_) {
+    return;
+  }
+
+  const bool top_chrome_pinned = IsZenModeTopChromePinned();
+  if (top_chrome_pinned) {
+    RevealZenModeTopChromeImmediately();
+  } else {
+    UpdateZenModeRevealFromCursor();
+  }
+
+  if (vertical_tab_strip_region_view_) {
+    vertical_tab_strip_region_view_->UpdateInteriorMargin();
+  }
+
+  if (multi_contents_view_) {
+    multi_contents_view_->MaybeUpdateContentsBorderAndOverlay();
+  }
+  UpdateZenCollapseButtonVisibility();
+  InvalidateLayout();
+}
+
+// Unpinned sidebar floats in zen mode, so we make sure that VTS is usable
+// in that state by expanding it when it's attached, not pinned, and collapsed.
+void BrowserView::MaybeExpandVerticalTabStripForZenMode() {
+  const bool vertical_initialized =
+      vertical_tab_strip_region_view_ &&
+      vertical_tab_strip_region_view_->GetTabStripView() != nullptr;
+  if (!zen_mode_enabled_ || IsZenModeSideChromePinned() ||
+      !ShouldDrawVerticalTabStrip() || !vertical_initialized) {
+    return;
+  }
+
+  auto* controller = tabs::VerticalTabStripStateController::From(browser_);
+  if (controller && controller->IsCollapsed()) {
+    controller->RequestCollapse(false);
+  }
+}
+
+void BrowserView::UpdateZenModeState() {
+  const bool zen_mode_enabled = IsZenModeEnabled();
+  UpdateZenModeEventMonitor();
+
+  if (zen_mode_enabled_ == zen_mode_enabled) {
+    if (zen_mode_enabled_) {
+      UpdateZenModeRevealFromCursor();
+    }
+    UpdateZenCollapseButtonVisibility();
+    return;
+  }
+
+  zen_mode_enabled_ = zen_mode_enabled;
+  chrome::BrowserCommandController::From(browser_)->UpdateCommandEnabled(
+      IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN, zen_mode_enabled);
+
+  if (zen_mode_enabled_) {
+    // When entering zen mode with pinned chrome, snap that chrome fully shown.
+    // Unpinned chrome should slide out from its current position.
+    if (IsZenModeSideChromePinned()) {
+      zen_side_chrome_animation_.Reset(1);
+    }
+
+    if (IsZenModeTopChromePinned()) {
+      zen_top_chrome_animation_.Reset(1);
+    }
+
+    UpdateZenModeReveal(/*reveal_top=*/false, /*reveal_side=*/false);
+  }
+
+  MaybeExpandVerticalTabStripForZenMode();
+  UpdateZenCollapseButtonVisibility();
+
+  if (!zen_mode_enabled_) {
+    if (auto* frame_view = GetFrameView()) {
+      frame_view->SetCaptionButtonsOpacity(1.0f);
+    }
+    if (!zen_top_chrome_animation_.IsShowing()) {
+      zen_top_chrome_animation_.Show();
+    }
+    if (!zen_side_chrome_animation_.IsShowing()) {
+      zen_side_chrome_animation_.Show();
+    }
+  }
+
+  if (multi_contents_view_) {
+    multi_contents_view_->MaybeUpdateContentsBorderAndOverlay();
+  }
+}
+
+void BrowserView::UpdateZenModeEventMonitor() {
+  if (!GetWidget() || !IsZenModeEnabled()) {
+    zen_mode_event_monitor_.reset();
+    return;
+  }
+
+  if (!zen_mode_event_monitor_) {
+    zen_mode_event_monitor_ = views::EventMonitor::CreateWindowMonitor(
+        this, GetWidget()->GetNativeWindow(),
+        {ui::EventType::kMouseMoved, ui::EventType::kMouseExited});
+  }
+}
+
+void BrowserView::StartZenCollapseTimer(base::TimeDelta delay) {
+  if (zen_cursor_exit_timer_.IsRunning()) {
+    return;
+  }
+  zen_cursor_exit_timer_.Start(
+      FROM_HERE, delay,
+      base::BindOnce(&BrowserView::UpdateZenModeReveal, base::Unretained(this),
+                     /*reveal_top=*/false, /*reveal_side=*/false));
+}
+
+bool BrowserView::IsLeavingZenSideGap(const gfx::Point& previous_point,
+                                      const gfx::Point& point) const {
+  if (!zen_side_chrome_animation_.IsShowing() || IsZenModeSideChromePinned() ||
+      !vertical_tab_strip_region_view_ ||
+      !vertical_tab_strip_region_view_->parent()) {
+    return false;
+  }
+
+  const gfx::Rect vts_bounds = views::View::ConvertRectToTarget(
+      vertical_tab_strip_region_view_->parent(), this,
+      vertical_tab_strip_region_view_->bounds());
+  if (IsVerticalTabStripRightAligned()) {
+    return previous_point.x() < vts_bounds.right() &&
+           point.x() >= vts_bounds.right() && point.x() < width();
+  }
+  return previous_point.x() >= vts_bounds.x() && point.x() >= 0 &&
+         point.x() < vts_bounds.x();
+}
+
+void BrowserView::UpdateZenModeRevealFromCursor() {
+  if (!zen_mode_enabled_) {
+    return;
+  }
+
+  gfx::Point cursor;
+  // When the cursor leaves the window, start a timer before collapsing.
+  // Brief cursor excursions outside the window shouldn't collapse
+  // the chrome.
+  gfx::Rect zen_hover_bounds = GetLocalBounds();
+  zen_hover_bounds.Outset(gfx::Outsets::VH(0, kZenModeRevealTriggerThickness));
+  if (!GetCursorLocationInBrowserView(&cursor) ||
+      !zen_hover_bounds.Contains(cursor)) {
+    zen_last_cursor_.reset();
+    StartZenCollapseTimer(kZenModeCursorExitGracePeriod);
+    return;
+  }
+
+  const std::optional<gfx::Point> previous_cursor = zen_last_cursor_;
+  zen_last_cursor_ = cursor;
+
+  const bool in_top_trigger = IsPointInZenTopTrigger(cursor);
+  const bool in_top_chrome = IsPointInZenTopChromeBounds(cursor);
+  const bool reveal_top = in_top_trigger || in_top_chrome;
+
+  // Top bar takes precedence: don't reveal side chrome when the cursor
+  // is in the top trigger zone or over the revealed top chrome.
+  const bool in_side_trigger = IsPointInZenSideTrigger(cursor);
+  const bool in_side_chrome = IsPointInZenSideChromeBounds(cursor);
+  const bool moving_out_through_side_gap =
+      previous_cursor && IsLeavingZenSideGap(*previous_cursor, cursor);
+  const bool reveal_side =
+      !reveal_top && !moving_out_through_side_gap &&
+      (in_side_trigger || in_side_chrome);
+
+  // Defer the collapse briefly so quick excursions don't cause the
+  // chrome to snap closed.
+  if (!reveal_top && !reveal_side) {
+    StartZenCollapseTimer(kZenModeHoverExitGracePeriod);
+    return;
+  }
+
+  zen_cursor_exit_timer_.Stop();
+  UpdateZenModeReveal(reveal_top, reveal_side);
+}
+
+void BrowserView::UpdateZenModeReveal(bool reveal_top, bool reveal_side) {
+  if (!zen_mode_enabled_) {
+    return;
+  }
+
+  if (!ShouldDrawVerticalTabStrip()) {
+    reveal_side = false;
+  }
+
+  // Pinned VTS stays visible regardless of cursor position.
+  if (IsZenModeSideChromePinned()) {
+    reveal_side = true;
+  }
+
+  // Pinned top chrome stays visible regardless of cursor position.
+  if (IsZenModeTopChromePinned()) {
+    reveal_top = true;
+  }
+
+  // Keep the top chrome visible while the omnibox is focused.
+  const bool omnibox_focused = toolbar_ && toolbar_->location_bar_view() &&
+                               toolbar_->location_bar_view()->HasFocus();
+  if (omnibox_focused) {
+    reveal_top = true;
+  }
+
+  // Keep the top chrome visible while a bubble/popup anchored inside the top
+  // container or a side panel is open.
+  if (!reveal_top && HasOpenBubbleFromTopContainer()) {
+    reveal_top = true;
+  }
+
+  // Menus are not BubbleDialogDelegate widgets, so handle the app menu
+  // explicitly.
+  if (!reveal_top) {
+    auto* toolbar_button_provider = ToolbarButtonProvider::From(browser_);
+    const AppMenuControl* app_menu_control =
+        toolbar_button_provider ? toolbar_button_provider->GetAppMenuControl()
+                                : nullptr;
+    if (app_menu_control && app_menu_control->IsMenuShowing()) {
+      reveal_top = true;
+    }
+  }
+
+  if (reveal_top) {
+    if (!zen_top_chrome_animation_.IsShowing()) {
+      zen_top_chrome_animation_.Show();
+      if (toolbar_) {
+        toolbar_->AbortToolbarContainerAnimationsForZenReveal();
+      }
+    }
+  } else if (zen_top_chrome_animation_.IsShowing()) {
+    zen_top_chrome_animation_.Hide();
+  }
+
+  if (reveal_side) {
+    if (!zen_side_chrome_animation_.IsShowing()) {
+      zen_side_chrome_animation_.Show();
+    }
+  } else if (zen_side_chrome_animation_.IsShowing()) {
+    zen_side_chrome_animation_.Hide();
+  }
+}
+
+void BrowserView::RevealZenModeTopChromeImmediately() {
+  zen_top_chrome_animation_.Reset(1);
+  if (vertical_tab_strip_region_view_) {
+    vertical_tab_strip_region_view_->UpdateInteriorMargin();
+  }
+  if (multi_contents_view_) {
+    multi_contents_view_->MaybeUpdateBorderForZenReveal();
+  }
+  if (auto* frame_view = GetFrameView()) {
+    frame_view->SetCaptionButtonsOpacity(1.0f);
+  }
+  InvalidateLayout();
+  DeprecatedLayoutImmediately();
+}
+
+bool BrowserView::IsZenModeEnabled() const {
+  auto* controller = HeliumLayoutStateController::From(browser_);
+  return controller && controller->IsZenModeEnabled() && GetIsNormalType() &&
+         GetSupportsTabStrip() && GetWidget() && !IsFullscreen();
+}
+
+void BrowserView::UpdateZenCollapseButtonVisibility() {
+  if (toolbar_) {
+    toolbar_->UpdateVerticalTabsCollapseButton();
+  }
+}
+
+bool BrowserView::IsPointInZenTopTrigger(const gfx::Point& point) const {
+  return point.y() >= 0 && point.y() < kZenModeRevealTriggerThickness;
+}
+
+bool BrowserView::IsPointInZenSideTrigger(const gfx::Point& point) const {
+  if (!ShouldDrawVerticalTabStrip()) {
+    return false;
+  }
+  auto* controller = tabs::VerticalTabStripStateController::From(browser_);
+  const bool right_aligned = controller && controller->IsTabStripRightAligned();
+  if (right_aligned) {
+    return point.x() >= width() - kZenModeRevealTriggerThickness &&
+           point.x() < width();
+  }
+  return point.x() >= 0 && point.x() < kZenModeRevealTriggerThickness;
+}
+
+namespace {
+
+bool IsOpenBubbleAnchoredInViewSubtree(views::Widget* widget,
+                                       const views::View* anchor_root) {
+  if (!widget || widget->IsClosed() || !widget->IsVisible()) {
+    return false;
+  }
+
+  views::WidgetDelegate* widget_delegate = widget->widget_delegate();
+  if (!widget_delegate) {
+    return false;
+  }
+
+  auto* bubble_delegate = widget_delegate->AsBubbleDialogDelegate();
+  if (!bubble_delegate) {
+    return false;
+  }
+
+  // Toasts are anchored to the top container, but since they're passive
+  // notifications, such as link copy success, we ignore them.
+  views::View* contents_view = bubble_delegate->GetContentsView();
+  if (contents_view &&
+      contents_view->GetProperty(views::kElementIdentifierKey) ==
+          toasts::ToastView::kToastViewId) {
+    return false;
+  }
+
+  views::View* anchor_view = bubble_delegate->GetAnchorView();
+  return anchor_view && anchor_root->Contains(anchor_view);
+}
+
+bool IsSidePanelOpen(const SidePanel* side_panel) {
+  if (!side_panel) {
+    return false;
+  }
+  switch (side_panel->state()) {
+    case SidePanel::State::kOpening:
+    case SidePanel::State::kOpen:
+      return true;
+    case SidePanel::State::kClosing:
+    case SidePanel::State::kClosed:
+      return false;
+  }
+  NOTREACHED();
+}
+
+}  // namespace
+
+bool BrowserView::HasOpenBubbleFromTopContainer() const {
+  if (IsSidePanelOpen(side_panel_)) {
+    return true;
+  }
+  if (!top_container_ || !GetWidget()) {
+    return false;
+  }
+  for (views::Widget* widget :
+       views::Widget::GetAllOwnedWidgets(GetWidget()->GetNativeView())) {
+    if (IsOpenBubbleAnchoredInViewSubtree(widget, top_container_)) {
+      return true;
+    }
+  }
+  return false;
+}
+
+void BrowserView::AppMenuShown() {
+  if (zen_mode_enabled_) {
+    UpdateZenModeReveal(/*reveal_top=*/true, /*reveal_side=*/false);
+  }
+}
+
+void BrowserView::AppMenuClosed() {
+  if (zen_mode_enabled_) {
+    UpdateZenModeRevealFromCursor();
+  }
+}
+
+bool BrowserView::IsPointInZenTopChromeBounds(const gfx::Point& point) const {
+  if (!top_container_ || !top_container_->parent()) {
+    return false;
+  }
+  gfx::Rect bounds = views::View::ConvertRectToTarget(
+      top_container_->parent(), this, top_container_->bounds());
+  // Extend the hit region to always include the full area from the top of
+  // the window to below the top container.  The top container's y-position
+  // changes during animation, so a fixed leeway above it would cause the
+  // cursor to flicker in and out of the region.
+  bounds.set_height(bounds.bottom() + kZenModeChromeHoverLeeway);
+  bounds.set_y(0);
+  bounds.Inset(gfx::Insets::VH(0, -kZenModeChromeHoverLeeway));
+  return bounds.Contains(point);
+}
+
+bool BrowserView::IsPointInZenSideChromeBounds(const gfx::Point& point) const {
+  if (!vertical_tab_strip_region_view_ ||
+      !vertical_tab_strip_region_view_->parent()) {
+    return false;
+  }
+  gfx::Rect bounds = views::View::ConvertRectToTarget(
+      vertical_tab_strip_region_view_->parent(), this,
+      vertical_tab_strip_region_view_->bounds());
+  bounds.Outset(
+      gfx::Outsets::VH(kZenModeChromeHoverLeeway, kZenModeChromeHoverLeeway));
+  return bounds.Contains(point);
+}
+
+bool BrowserView::GetCursorLocationInBrowserView(
+    gfx::Point* cursor_location) const {
+  if (!zen_mode_event_monitor_) {
+    return false;
+  }
+  *cursor_location = zen_mode_event_monitor_->GetLastMouseLocation();
+  views::View::ConvertPointFromScreen(this, cursor_location);
+  return true;
+}
+
 void BrowserView::OnOrganizerPanelStateChanged(
     OrganizerPanelStateController* controller) {
   organizer_panel_container_->OnOrganizerPanelStateChanged(controller);
@@ -2425,6 +2939,8 @@ void BrowserView::FullscreenStateChanged
                     !GetFocusManager()->GetFocusedView());
     }
   }
+
+  UpdateZenModeState();
 }
 
 ToolbarButtonProvider* BrowserView::toolbar_button_provider() {
@@ -2454,6 +2970,13 @@ void BrowserView::SetFocusToLocationBar(
     return;
   }
 
+  // In zen mode, snap the toolbar to fully shown so the omnibox is visible
+  // and can receive focus.  A synchronous layout is required so the toolbar
+  // has non-zero height before FocusLocation runs.
+  if (zen_mode_enabled_) {
+    RevealZenModeTopChromeImmediately();
+  }
+
   LocationBar* location_bar = GetLocationBar();
   location_bar->FocusLocation(is_user_initiated,
                               /*clear_focus_if_failed=*/true);
@@ -3182,7 +3705,7 @@ bool BrowserView::IsToolbarVisible() con
 }
 
 bool BrowserView::IsToolbarShowing() const {
-  return GetTabStripVisible();
+  return IsToolbarVisible();
 }
 
 bool BrowserView::IsLocationBarVisible() const {
@@ -4278,6 +4801,11 @@ void BrowserView::OnWidgetActivationChan
     }
   }
 
+  // Collapse zen mode chrome when the window is deactivated.
+  if (!active && zen_mode_enabled_) {
+    UpdateZenModeReveal(/*reveal_top=*/false, /*reveal_side=*/false);
+  }
+
   browser_->GetFeatures()
       .extension_keybinding_registry()
       ->OnHostActivationChanged(active);
@@ -5163,6 +5691,8 @@ void BrowserView::AddedToWidget() {
             base::BindRepeating(&BrowserView::OnToolbarTabStripStateChanged,
                                 base::Unretained(this)));
     OnToolbarTabStripStateChanged(helium_layout_controller);
+  } else {
+    UpdateZenModeState();
   }
 
   UpdateTabSearchBubbleHost();
@@ -5248,6 +5778,9 @@ void BrowserView::AddedToWidget() {
   // once per browser instance.
   auto* toolbar_button_provider = ToolbarButtonProvider::From(browser_);
   CHECK(toolbar_button_provider);
+  if (auto* app_menu_control = toolbar_button_provider->GetAppMenuControl()) {
+    app_menu_control_observation_.Observe(app_menu_control);
+  }
 
   // `AutofillBubbleHandlerImpl` depends on the ToolbarButtonProvider so it must
   // be constructed following the check above.
@@ -5285,6 +5818,7 @@ void BrowserView::AddedToWidget() {
           tabs::VerticalTabStripStateController::From(browser_)) {
     if (vertical_tab_strip_state_controller->ShouldDisplayVerticalTabs()) {
       vertical_tab_strip_region_view_->InitializeTabStrip();
+      MaybeExpandVerticalTabStripForZenMode();
     } else {
       horizontal_tab_strip_region_view_->InitializeTabStrip();
     }
@@ -5299,6 +5833,8 @@ void BrowserView::AddedToWidget() {
 }
 
 void BrowserView::RemovedFromWidget() {
+  zen_mode_event_monitor_.reset();
+  app_menu_control_observation_.Reset();
   CHECK(GetFocusManager());
 #if BUILDFLAG(IS_WIN)
   pip_exclusion_observer_.reset();
@@ -5331,6 +5867,14 @@ void BrowserView::OnThemeChanged() {
   }
 
   FrameColorsChanged();
+
+  // Theme changes may recreate caption buttons, resetting their opacity.
+  // Reapply zen mode state so buttons stay hidden during zen mode.
+  if (zen_mode_enabled_) {
+    if (auto* frame_view = GetFrameView()) {
+      frame_view->SetCaptionButtonsOpacity(GetTopChromeRevealRatio());
+    }
+  }
 }
 
 bool BrowserView::GetDropFormats(
@@ -5604,6 +6148,9 @@ void BrowserView::UpdateFastResizeForCon
 }
 
 int BrowserView::GetClientAreaTop() {
+  if (!IsToolbarVisible()) {
+    return 0;
+  }
   views::View* top_view = toolbar_;
 
   // Get the top of the top view in browser view coordinates.
@@ -6212,6 +6759,56 @@ void BrowserView::OnWillChangeFocus(View
 }
 void BrowserView::OnDidChangeFocus(View* focused_before, View* focused_now) {
   UpdateAccessibleNameForRootView();
+  if (zen_mode_enabled_) {
+    UpdateZenModeRevealFromCursor();
+  }
+}
+
+void BrowserView::OnEvent(const ui::Event& event) {
+  if (!zen_mode_enabled_ || !event.IsMouseEvent()) {
+    return;
+  }
+  // Mouse exit events can also come from internal view transitions. Only use
+  // the longer window-exit grace period after confirming the cursor left the
+  // BrowserView bounds.
+  if (event.type() == ui::EventType::kMouseExited) {
+    gfx::Point cursor;
+    if (GetCursorLocationInBrowserView(&cursor) &&
+        GetLocalBounds().Contains(cursor)) {
+      UpdateZenModeRevealFromCursor();
+      return;
+    }
+    zen_last_cursor_.reset();
+    StartZenCollapseTimer(kZenModeCursorExitGracePeriod);
+    return;
+  }
+  UpdateZenModeRevealFromCursor();
+}
+
+void BrowserView::AnimationProgressed(const gfx::Animation* animation) {
+  if (animation == &zen_top_chrome_animation_ ||
+      animation == &zen_side_chrome_animation_) {
+    const bool top_chrome_animation = animation == &zen_top_chrome_animation_;
+    if (top_chrome_animation && vertical_tab_strip_region_view_) {
+      vertical_tab_strip_region_view_->UpdateInteriorMargin();
+    }
+    if (top_chrome_animation && multi_contents_view_) {
+      multi_contents_view_->MaybeUpdateBorderForZenReveal();
+    }
+    InvalidateLayout();
+    if (top_chrome_animation) {
+      if (auto* frame_view = GetFrameView()) {
+        // Fade caption buttons with the toolbar.  The toolbar still reserves
+        // their space via exclusion areas since the buttons remain visible to
+        // the layout system (only their rendering opacity is changed).
+        frame_view->SetCaptionButtonsOpacity(GetTopChromeRevealRatio());
+      }
+    }
+  }
+}
+
+void BrowserView::AnimationEnded(const gfx::Animation* animation) {
+  AnimationProgressed(animation);
 }
 
 WebAppFrameToolbarView* BrowserView::web_app_frame_toolbar() {
--- a/chrome/browser/ui/views/frame/browser_view.h
+++ b/chrome/browser/ui/views/frame/browser_view.h
@@ -30,6 +30,7 @@
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
 #include "chrome/browser/ui/translate/partial_translate_bubble_model.h"
 #include "chrome/browser/ui/user_education/browser_user_education_interface.h"
+#include "chrome/browser/ui/views/frame/app_menu_button_observer.h"
 #include "chrome/browser/ui/views/frame/browser_widget.h"
 #include "chrome/browser/ui/views/frame/contents_container_view.h"
 #include "chrome/browser/ui/views/frame/contents_web_view.h"
@@ -57,6 +58,9 @@
 #include "ui/base/metadata/metadata_header_macros.h"
 #include "ui/base/mojom/window_show_state.mojom-forward.h"
 #include "ui/base/pointer/touch_ui_controller.h"
+#include "ui/events/event_observer.h"
+#include "ui/gfx/animation/animation_delegate.h"
+#include "ui/gfx/animation/slide_animation.h"
 #include "ui/gfx/native_ui_types.h"
 #include "ui/views/controls/button/button.h"
 #include "ui/views/controls/webview/unhandled_keyboard_event_handler.h"
@@ -73,6 +77,7 @@
 // view: http://dev.chromium.org/developers/design-documents/browser-window
 
 class AccessibilityFocusHighlight;
+class AppMenuControl;
 class BookmarkBarController;
 class BookmarkBarView;
 class Browser;
@@ -118,6 +123,7 @@ enum class Channel;
 }
 
 namespace views {
+class EventMonitor;
 class WebView;
 }  // namespace views
 
@@ -138,10 +144,17 @@ class BrowserView : public BrowserWindow
                     public ImmersiveModeController::Observer,
                     public browser_shortcuts::BrowserShortcutService::Observer,
                     public views::FocusChangeListener,
-                    public BookmarkBarController::Delegate {
+                    public BookmarkBarController::Delegate,
+                    public ui::EventObserver,
+                    public gfx::AnimationDelegate,
+                    public AppMenuButtonObserver {
   METADATA_HEADER(BrowserView, views::ClientView)
 
  public:
+  // Unhide views::View::OnEvent (takes ui::Event*) which is shadowed by
+  // ui::EventObserver::OnEvent (takes const ui::Event&).
+  using views::View::OnEvent;
+
   // The name of a key to store on the window handle so that other code can
   // locate this object using just the handle.
   static constexpr char kBrowserViewKey[] = "__BROWSER_VIEW__";
@@ -408,6 +421,17 @@ class BrowserView : public BrowserWindow
   // Returns the current shown ratio of the top browser controls.
   float GetTopControlsSlideBehaviorShownRatio() const;
 
+  // Returns reveal ratios used by BrowserViewLayout for zen mode transitions.
+  bool IsZenModeEnabledForLayout() const;
+  float GetTopChromeRevealRatio() const;
+  float GetSideChromeRevealRatio() const;
+  bool IsZenModeSideChromePinned() const;
+  bool IsZenModeTopChromePinned() const;
+
+  // Toggles the side chrome (VTS) visibility in zen mode.
+  void ToggleZenModeSideChrome();
+  void ToggleZenModeTopChrome();
+
   // Returns the widget for anchoring bubbles and dialogs.
   // This returns BrowserWidget except on fullscreen macOS where the toolbar is
   // hosted in an OverlayWidget.
@@ -747,6 +771,17 @@ class BrowserView : public BrowserWindow
       BookmarkBar::AnimateChangeType change_type) override;
   void OnFocusBookmarksToolbar() override;
 
+  // ui::EventObserver:
+  void OnEvent(const ui::Event& event) override;
+
+  // gfx::AnimationDelegate:
+  void AnimationProgressed(const gfx::Animation* animation) override;
+  void AnimationEnded(const gfx::Animation* animation) override;
+
+  // AppMenuButtonObserver:
+  void AppMenuShown() override;
+  void AppMenuClosed() override;
+
   // Testing interface:
   BrowserViewLayout* GetBrowserViewLayoutForTesting() {
     return GetBrowserViewLayout();
@@ -888,6 +923,27 @@ class BrowserView : public BrowserWindow
 
   void OnOrganizerPanelStateChanged(OrganizerPanelStateController* controller);
 
+  // Zen mode helpers.
+  void OnZenModeSideChromePinnedPrefChanged();
+  void OnZenModeTopChromePinnedPrefChanged();
+  void UpdateZenModeState();
+  void MaybeExpandVerticalTabStripForZenMode();
+  void UpdateZenCollapseButtonVisibility();
+  void UpdateZenModeEventMonitor();
+  void StartZenCollapseTimer(base::TimeDelta delay);
+  bool IsLeavingZenSideGap(const gfx::Point& previous_point,
+                           const gfx::Point& point) const;
+  void UpdateZenModeRevealFromCursor();
+  void UpdateZenModeReveal(bool reveal_top, bool reveal_side);
+  void RevealZenModeTopChromeImmediately();
+  bool IsZenModeEnabled() const;
+  bool IsPointInZenTopTrigger(const gfx::Point& point) const;
+  bool IsPointInZenSideTrigger(const gfx::Point& point) const;
+  bool IsPointInZenTopChromeBounds(const gfx::Point& point) const;
+  bool IsPointInZenSideChromeBounds(const gfx::Point& point) const;
+  bool HasOpenBubbleFromTopContainer() const;
+  bool GetCursorLocationInBrowserView(gfx::Point* cursor_location) const;
+
   // Callback for the loading animation(s) associated with this view.
   void LoadingAnimationTimerCallback();
   void LoadingAnimationCallback(base::TimeTicks timestamp);
@@ -1342,6 +1398,9 @@ class BrowserView : public BrowserWindow
   base::ScopedObservation<views::Widget, views::WidgetObserver>
       widget_observation_{this};
 
+  base::ScopedObservation<AppMenuControl, AppMenuButtonObserver>
+      app_menu_control_observation_{this};
+
   bool interactive_resize_in_progress_ = false;
 
   // The last bounds we notified about in TryNotifyWindowBoundsChanged().
@@ -1404,6 +1463,14 @@ class BrowserView : public BrowserWindow
 
   std::unique_ptr<TabCyclingEventHandler> tab_cycling_event_handler_;
 
+  // Zen mode state.
+  std::unique_ptr<views::EventMonitor> zen_mode_event_monitor_;
+  gfx::SlideAnimation zen_top_chrome_animation_;
+  gfx::SlideAnimation zen_side_chrome_animation_;
+  base::OneShotTimer zen_cursor_exit_timer_;
+  std::optional<gfx::Point> zen_last_cursor_;
+  bool zen_mode_enabled_ = false;
+
   mutable base::WeakPtrFactory<BrowserView> weak_ptr_factory_{this};
 };
 
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate.h
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate.h
@@ -56,6 +56,11 @@ class BrowserViewLayoutDelegate {
   virtual ExclusiveAccessBubbleViews* GetExclusiveAccessBubble() const = 0;
   virtual bool IsTopControlsSlideBehaviorEnabled() const = 0;
   virtual float GetTopControlsSlideBehaviorShownRatio() const = 0;
+  virtual bool IsZenModeEnabledForLayout() const { return false; }
+  virtual float GetTopChromeRevealRatio() const { return 1.f; }
+  virtual float GetSideChromeRevealRatio() const { return 1.f; }
+  virtual bool IsZenModeSideChromePinned() const { return false; }
+  virtual bool IsZenModeTopChromePinned() const { return false; }
   virtual gfx::NativeView GetHostViewForAnchoring() const = 0;
   virtual bool HasFindBarController() const = 0;
   virtual void MoveWindowForFindBarIfNecessary() const = 0;
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.cc
@@ -212,6 +212,22 @@ float BrowserViewLayoutDelegateImpl::Get
   return browser_view_->GetTopControlsSlideBehaviorShownRatio();
 }
 
+bool BrowserViewLayoutDelegateImpl::IsZenModeEnabledForLayout() const {
+  return browser_view_->IsZenModeEnabledForLayout();
+}
+
+float BrowserViewLayoutDelegateImpl::GetTopChromeRevealRatio() const {
+  return browser_view_->GetTopChromeRevealRatio();
+}
+
+float BrowserViewLayoutDelegateImpl::GetSideChromeRevealRatio() const {
+  return browser_view_->GetSideChromeRevealRatio();
+}
+
+bool BrowserViewLayoutDelegateImpl::IsZenModeTopChromePinned() const {
+  return browser_view_->IsZenModeTopChromePinned();
+}
+
 gfx::NativeView BrowserViewLayoutDelegateImpl::GetHostViewForAnchoring() const {
   return browser_view_->GetWidgetForAnchoring()->GetNativeView();
 }
@@ -259,6 +275,10 @@ bool BrowserViewLayoutDelegateImpl::Shou
   return true;
 }
 
+bool BrowserViewLayoutDelegateImpl::IsZenModeSideChromePinned() const {
+  return browser_view_->IsZenModeSideChromePinned();
+}
+
 int BrowserViewLayoutDelegateImpl::GetExtraInfobarOffset() const {
 #if BUILDFLAG(IS_MAC)
   auto* const controller = GetImmersiveModeController();
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.h
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_delegate_impl.h
@@ -44,6 +44,11 @@ class BrowserViewLayoutDelegateImpl : pu
   ExclusiveAccessBubbleViews* GetExclusiveAccessBubble() const override;
   bool IsTopControlsSlideBehaviorEnabled() const override;
   float GetTopControlsSlideBehaviorShownRatio() const override;
+  bool IsZenModeEnabledForLayout() const override;
+  float GetTopChromeRevealRatio() const override;
+  float GetSideChromeRevealRatio() const override;
+  bool IsZenModeSideChromePinned() const override;
+  bool IsZenModeTopChromePinned() const override;
   gfx::NativeView GetHostViewForAnchoring() const override;
   bool HasFindBarController() const override;
   void MoveWindowForFindBarIfNecessary() const override;
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout_impl.cc
@@ -93,10 +93,10 @@ void BrowserViewLayoutImpl::ProposedLayo
       // results in a potential double-layout, or in extreme cases, bugs like
       // https://crbug.com/464220949
       std::move(it->second).ApplyLayout(child, set_view_visibility);
-      child->SetBoundsRect(it->second.bounds);
       if (it->second.visibility) {
         set_view_visibility(child, *it->second.visibility);
       }
+      child->SetBoundsRect(it->second.bounds);
       children.erase(it);
     }
   }
@@ -317,9 +317,27 @@ void BrowserViewLayoutImpl::ConfigureTop
     // When in immersive mode, top container is painted with the toolbar theme.
     background->SetVisible(true);
     background->SetPrimaryColor(CustomCornersBackground::ToolbarTheme());
-  } else {
+  }
+#if BUILDFLAG(IS_LINUX)
+  // On Linux with classic theme (no native top border), paint the top
+  // container with the frame color and rounded upper corners in zen mode
+  // so the toolbar doesn't poke above the window's rounded corners.
+  // Not needed on Mac (AppKit titlebar) or GTK/Qt (native frame border).
+  else if (delegate().IsZenModeEnabledForLayout() &&
+           views().browser_view->bounds().y() == 0) {
+    background->SetVisible(true);
+    background->SetPrimaryColor(CustomCornersBackground::FrameTheme());
+    const auto window_corner = background->GetWindowCorner(/*upper=*/true);
+    CustomCornersBackground::Corners corners;
+    corners[CornerOrientation::kTopLeading] = window_corner;
+    corners[CornerOrientation::kTopTrailing] = window_corner;
+    background->SetCorners(corners);
+  }
+#endif  // BUILDFLAG(IS_LINUX)
+  else {
     // No need to paint otherwise.
     background->SetVisible(false);
+    background->SetCorners({});
   }
 }
 
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -38,6 +38,7 @@
 #include "chrome/browser/ui/views/frame/tab_strip_region_view.h"
 #include "chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.h"
 #include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
+#include "chrome/browser/ui/views/helium/frame_corner_radius.h"
 #include "chrome/browser/ui/views/infobars/infobar_container_view.h"
 #include "chrome/browser/ui/views/side_panel/side_panel.h"
 #include "chrome/browser/ui/views/tabs/organizer/layout_constants.h"
@@ -67,6 +68,88 @@ constexpr int kVerticalTabsGrabHandleSiz
 // against the frame controls when it is the top-most row.
 constexpr int kTopToolbarExclusion = 10;
 
+constexpr int kZenModeVTSPadding = 8;
+
+struct ZenModeLayoutState {
+  bool enabled = false;
+  bool floating_vertical_tab_strip = false;
+  float top_chrome_ratio = 1.0f;
+  float side_chrome_ratio = 1.0f;
+  int vertical_tab_strip_padding = 0;
+  int vertical_tab_strip_top_padding = 0;
+
+  bool TopChromeVisible() const { return top_chrome_ratio > 0.0f; }
+
+  bool ShouldLayoutVerticalTabStrip() const {
+    return !floating_vertical_tab_strip || side_chrome_ratio > 0.0f;
+  }
+};
+
+int ScaleExtent(int extent, float ratio) {
+  return base::ClampRound(std::max(0, extent) * ratio);
+}
+
+gfx::Rect SlideBoundsFromTop(const gfx::Rect& bounds, float ratio) {
+  gfx::Rect slid(bounds);
+  // Slide from fully above the parent's top edge to the view's shown bounds.
+  slid.Offset(0, ScaleExtent(bounds.bottom(), ratio) - bounds.bottom());
+  return slid;
+}
+
+int GetDynamicTabStripToolbarBottom(const views::View& toolbar,
+                                    const gfx::Rect& toolbar_bounds,
+                                    bool zen_mode,
+                                    float top_chrome_ratio) {
+  // The toolbar bounds may already be slid upward during zen reveal. Use the
+  // stable shown height for the dynamic tab strip target instead of deriving a
+  // target from animated bounds.
+  if (zen_mode && top_chrome_ratio < 1.0f) {
+    return toolbar.GetPreferredSize().height();
+  }
+  return toolbar_bounds.bottom();
+}
+
+gfx::Rect SlideBoundsFromSide(const gfx::Rect& bounds,
+                              const gfx::Rect& visual_client_area,
+                              float ratio,
+                              bool right_aligned) {
+  gfx::Rect slid(bounds);
+  const int hidden_x = right_aligned ? visual_client_area.right()
+                                     : visual_client_area.x() - bounds.width();
+  slid.set_x(hidden_x + base::ClampRound((bounds.x() - hidden_x) * ratio));
+  return slid;
+}
+
+ZenModeLayoutState GetZenModeLayoutState(
+    const BrowserViewLayoutDelegate& delegate,
+    bool vertical_tab_strip_active) {
+  ZenModeLayoutState state;
+  state.enabled = delegate.IsZenModeEnabledForLayout();
+  state.floating_vertical_tab_strip = state.enabled &&
+                                      vertical_tab_strip_active &&
+                                      !delegate.IsZenModeSideChromePinned();
+  state.top_chrome_ratio = delegate.GetTopChromeRevealRatio();
+  state.side_chrome_ratio = delegate.GetSideChromeRevealRatio();
+
+  if (!state.floating_vertical_tab_strip) {
+    return state;
+  }
+
+  state.vertical_tab_strip_padding = kZenModeVTSPadding;
+  const int revealed_top_padding =
+      std::max(0, GetGenericFrameRadius() -
+                      GetLayoutConstant(LayoutConstant::kHeliumBasePadding));
+  if (delegate.IsZenModeTopChromePinned()) {
+    state.vertical_tab_strip_top_padding = revealed_top_padding;
+  } else {
+    state.vertical_tab_strip_top_padding =
+        ScaleExtent(state.vertical_tab_strip_padding,
+                    1.0f - state.top_chrome_ratio) +
+        ScaleExtent(revealed_top_padding, state.top_chrome_ratio);
+  }
+  return state;
+}
+
 // Maximum portion of the window a "size-restricted" contents-height side panel
 // can take up. This is not the only limit on side panel size.
 constexpr float kMaxContentsHeightSidePanelFraction = 2.f / 3.f;
@@ -716,6 +799,8 @@ BrowserViewTabbedLayoutImpl::CalculatePr
       IsDynamicTabStrip(layout_data_->tab_strip_type);
   const bool show_dynamic_tab_strip =
       ShowDynamicTabStrip(layout_data_->tab_strip_type);
+  const ZenModeLayoutState zen = GetZenModeLayoutState(
+      delegate(), layout_data_->tab_strip_type == TabStripType::kVertical);
   std::optional<int> dynamic_tab_strip_top;
   const HorizontalLayout& horizontal_layout = layout_data_->horizontal_layout;
   const bool infobar_visible = delegate().IsInfobarVisible();
@@ -734,14 +819,20 @@ BrowserViewTabbedLayoutImpl::CalculatePr
       const int leading_margin = GetHorizontalTabStripLeadingMargin(params);
       tabstrip_bounds = GetBoundsWithExclusion(
           params, views().horizontal_tab_strip_region_view, leading_margin);
-      const int default_tabstrip_toolbar_overlap =
-          GetLayoutConstant(LayoutConstant::kHeliumBasePadding);
-      params.SetTop(tabstrip_bounds.bottom() -
-                    default_tabstrip_toolbar_overlap);
+      tabstrip_bounds =
+          SlideBoundsFromTop(tabstrip_bounds, zen.top_chrome_ratio);
+      if (tabstrip_bounds.height() > 0) {
+        const int default_tabstrip_toolbar_overlap =
+            ScaleExtent(GetLayoutConstant(LayoutConstant::kHeliumBasePadding),
+                        zen.top_chrome_ratio);
+        params.SetTop(tabstrip_bounds.bottom() -
+                      default_tabstrip_toolbar_overlap);
+      }
       needs_exclusion = false;
     }
     layout.AddChild(views().horizontal_tab_strip_region_view, tabstrip_bounds,
-                    layout_data_->tab_strip_type == TabStripType::kHorizontal);
+                    layout_data_->tab_strip_type == TabStripType::kHorizontal &&
+                        tabstrip_bounds.height() > 0);
   }
 
   // This will be the area next to the vertical tab strip (if present) that will
@@ -757,14 +848,7 @@ BrowserViewTabbedLayoutImpl::CalculatePr
   if (IsParentedTo(views().vertical_tab_strip_region_view,
                    views().browser_view)) {
     if (layout_data_->tab_strip_type == TabStripType::kVertical) {
-      const int vertical_tab_strip_hover_width =
-          std::max(0, tabs::kVerticalTabStripDefaultUncollapsedWidth -
-                          horizontal_layout.vertical_tab_strip_width) *
-          vertical_tab_strip_animation.expand_on_hover_width;
-
-      int vertical_tab_strip_width =
-          horizontal_layout.vertical_tab_strip_width +
-          vertical_tab_strip_hover_width;
+      int vertical_tab_strip_width = horizontal_layout.vertical_tab_strip_width;
 
       // The overall width during an expand must change monotonically.
       if (vertical_tab_strip_animation.current_motion ==
@@ -786,15 +870,30 @@ BrowserViewTabbedLayoutImpl::CalculatePr
 
       const int vertical_tab_strip_x =
           vertical_right_aligned
-              ? params.visual_client_area.right() - vertical_tab_strip_width
-              : params.visual_client_area.x();
-      vertical_tab_strip_bounds =
-          gfx::Rect(vertical_tab_strip_x,
-                    params.visual_client_area.y() +
-                        vertical_tab_strip_animation.top_offset,
-                    vertical_tab_strip_width,
-                    params.visual_client_area.height() -
-                        vertical_tab_strip_animation.top_offset);
+              ? params.visual_client_area.right() -
+                    zen.vertical_tab_strip_padding - vertical_tab_strip_width
+              : params.visual_client_area.x() +
+                    zen.vertical_tab_strip_padding;
+
+      int top_offset = vertical_tab_strip_animation.top_offset;
+      if (zen.enabled) {
+        top_offset = ScaleExtent(top_offset, zen.top_chrome_ratio);
+      }
+
+      const int vertical_tab_strip_height =
+          std::max(0, params.visual_client_area.height() - top_offset -
+                          zen.vertical_tab_strip_top_padding -
+                          zen.vertical_tab_strip_padding);
+      vertical_tab_strip_bounds = {
+          vertical_tab_strip_x,
+          params.visual_client_area.y() + top_offset +
+              zen.vertical_tab_strip_top_padding,
+          vertical_tab_strip_width, vertical_tab_strip_height};
+      if (zen.floating_vertical_tab_strip) {
+        vertical_tab_strip_bounds = SlideBoundsFromSide(
+            vertical_tab_strip_bounds, params.visual_client_area,
+            zen.side_chrome_ratio, vertical_right_aligned);
+      }
 
       if (layout_data_->window_state == WindowState::kFullscreenWithToolbar &&
           layout_data_->separator_info.adjust_for_missing_separator) {
@@ -802,17 +901,23 @@ BrowserViewTabbedLayoutImpl::CalculatePr
             gfx::Insets::TLBR(views::Separator::kThickness, 0, 0, 0));
       }
 
-      const int inset_amount = horizontal_layout.vertical_tab_strip_width -
-                               GetVerticalTabStripContentOverlap();
-      params.InsetHorizontal(inset_amount, /*leading=*/!vertical_right_aligned);
+      vertical_tab_strip_width = vertical_tab_strip_bounds.width();
+      if (!zen.floating_vertical_tab_strip) {
+        const int inset_amount =
+            vertical_tab_strip_width - GetVerticalTabStripContentOverlap();
+        params.InsetHorizontal(inset_amount,
+                               /*leading=*/!vertical_right_aligned);
+      }
 
       // Let the vertical tab strip animate out over the content.
       if (vertical_tab_strip_animation.current_motion) {
-        clip_content_for_animation =
+        const bool vertical_tab_strip_resize_animation =
             vertical_tab_strip_animation.current_motion ==
                 TabStripAnimations::kExpand ||
             vertical_tab_strip_animation.current_motion ==
                 TabStripAnimations::kCollapse;
+        clip_content_for_animation =
+            clip_content_for_animation || vertical_tab_strip_resize_animation;
         InsetHorizontal(unclipped_contents_region,
                         VerticalTabStripRegionView::kCollapsedWidth,
                         !vertical_right_aligned);
@@ -824,7 +929,8 @@ BrowserViewTabbedLayoutImpl::CalculatePr
     }
     layout.AddChild(views().vertical_tab_strip_region_view,
                     vertical_tab_strip_bounds,
-                    layout_data_->tab_strip_type == TabStripType::kVertical);
+                    layout_data_->tab_strip_type == TabStripType::kVertical &&
+                        zen.ShouldLayoutVerticalTabStrip());
   }
 
   if (IsParentedTo(views().vertical_tab_strip_background_blur_backdrop,
@@ -888,7 +994,8 @@ BrowserViewTabbedLayoutImpl::CalculatePr
     top_container_layout.bounds = GetTopContainerBoundsInParent(
         top_container_local_bounds, parent_params);
 
-    if (layout_data_->tab_strip_type == TabStripType::kVertical) {
+    if (layout_data_->tab_strip_type == TabStripType::kVertical &&
+        !zen.floating_vertical_tab_strip) {
       auto& top_container_children = top_container_layout.children;
       auto bookmarks_child = top_container_children.find(views().bookmark_bar);
       if (bookmarks_child != top_container_children.end() &&
@@ -922,15 +1029,40 @@ BrowserViewTabbedLayoutImpl::CalculatePr
       if (const auto* toolbar_layout =
               top_container_layout.GetLayoutFor(views().toolbar);
           toolbar_layout && !toolbar_layout->bounds.IsEmpty()) {
+        const int toolbar_bottom = GetDynamicTabStripToolbarBottom(
+            *views().toolbar, toolbar_layout->bounds, zen.enabled,
+            zen.top_chrome_ratio);
         dynamic_tab_strip_top = top_container_layout.bounds.y() +
-                                toolbar_layout->bounds.bottom() -
-                                toolbar_tabstrip_overlap;
+                                toolbar_bottom - toolbar_tabstrip_overlap;
       } else {
         dynamic_tab_strip_top = top_container_layout.bounds.y();
       }
     }
 
     params.SetTop(top_container_layout.bounds.bottom());
+
+    if (zen.enabled) {
+      AdjustTopContainerForZenMode(top_container_layout, params,
+                                   zen.top_chrome_ratio);
+    }
+
+    // The floating VTS offset only accounts for toolbar height, but the
+    // top container may be taller (e.g. bookmarks bar, zen border strip).
+    // Ensure the floating VTS starts below the full top container.
+    if (zen.floating_vertical_tab_strip && zen.TopChromeVisible() &&
+        top_container_layout.bounds.height() > 0) {
+      auto it = layout.children.find(views().vertical_tab_strip_region_view);
+      if (it != layout.children.end()) {
+        const int tc_bottom = top_container_layout.bounds.bottom();
+        const int floating_top = tc_bottom + zen.vertical_tab_strip_top_padding;
+        if (it->second.bounds.y() < floating_top) {
+          it->second.bounds.set_y(floating_top);
+          it->second.bounds.set_height(
+              std::max(0, browser_params.visual_client_area.bottom() -
+                              floating_top - zen.vertical_tab_strip_padding));
+        }
+      }
+    }
   }
 
   if (dynamic_tab_strip) {
@@ -938,15 +1070,21 @@ BrowserViewTabbedLayoutImpl::CalculatePr
 
     const int tabstrip_height =
         views().horizontal_tab_strip_region_view->GetPreferredSize().height();
-    const int tabstrip_top =
-        show_dynamic_tab_strip
-            ? dynamic_tab_strip_top.value_or(params.visual_client_area.y())
-            : -tabstrip_height;
-    const gfx::Rect tabstrip_bounds(params.visual_client_area.x(), tabstrip_top,
-                                    params.visual_client_area.width(),
-                                    tabstrip_height);
+    gfx::Rect tabstrip_bounds;
+    if (show_dynamic_tab_strip) {
+      tabstrip_bounds = gfx::Rect(
+          params.visual_client_area.x(),
+          dynamic_tab_strip_top.value_or(params.visual_client_area.y()),
+          params.visual_client_area.width(), tabstrip_height);
+      tabstrip_bounds =
+          SlideBoundsFromTop(tabstrip_bounds, zen.top_chrome_ratio);
+    } else {
+      tabstrip_bounds =
+          gfx::Rect(params.visual_client_area.x(), -tabstrip_height,
+                    params.visual_client_area.width(), tabstrip_height);
+    }
     layout.AddChild(views().horizontal_tab_strip_region_view, tabstrip_bounds,
-                    show_dynamic_tab_strip);
+                    show_dynamic_tab_strip && tabstrip_bounds.height() > 0);
   }
 
   // Lay out the main area background.
@@ -974,6 +1112,8 @@ BrowserViewTabbedLayoutImpl::CalculatePr
   bool side_panel_leading = false;
   bool leading_side_chrome_attached = false;
   bool trailing_side_chrome_attached = false;
+  bool leading_vertical_tab_strip_attached = false;
+  bool trailing_vertical_tab_strip_attached = false;
   const double side_panel_reveal_amount =
       horizontal_layout.has_side_panel()
           ? views().side_panel->GetAnimationValue()
@@ -1076,11 +1216,14 @@ BrowserViewTabbedLayoutImpl::CalculatePr
     params.SetTop(top_container_layout.bounds.bottom());
   }
 
-  if (layout_data_->tab_strip_type == TabStripType::kVertical) {
+  if (layout_data_->tab_strip_type == TabStripType::kVertical &&
+      !zen.floating_vertical_tab_strip) {
     if (delegate().IsVerticalTabStripRightAligned()) {
       trailing_side_chrome_attached = true;
+      trailing_vertical_tab_strip_attached = true;
     } else {
       leading_side_chrome_attached = true;
+      leading_vertical_tab_strip_attached = true;
     }
   }
 
@@ -1119,14 +1262,21 @@ BrowserViewTabbedLayoutImpl::CalculatePr
   const auto& separator_info = layout_data_->separator_info;
   views().multi_contents_view->SetTopChromeAttachmentState(
       infobar_visible && should_use_rounded_frame,
-      separator_info.multi_contents_separator);
+      separator_info.multi_contents_separator &&
+          (!zen.enabled || zen.TopChromeVisible()));
 
   // Updating the top, left and right insets for contents container view when
   // in split view. This is dependent on a number of other browser states.
   if (horizontal_layout.is_split_view) {
-    const bool include_top_inset =
+    const int top_inset =
         layout_data_->window_state == WindowState::kFullscreen ||
-        infobar_visible;
+                infobar_visible
+            ? MultiContentsView::kSplitViewContentInset
+            : zen.enabled
+                  ? ScaleExtent(MultiContentsView::kSplitViewContentInset,
+                                std::clamp(1.0f - zen.top_chrome_ratio, 0.0f,
+                                           1.0f))
+                  : 0;
     const bool include_leading_inset =
         separator_info.shadow_box ||
         !(horizontal_layout.has_side_panel() && side_panel_leading);
@@ -1136,11 +1286,17 @@ BrowserViewTabbedLayoutImpl::CalculatePr
     const int default_inset = MultiContentsView::kSplitViewContentInset;
     const int panel_side_inset =
         base::ClampRound((1.0 - side_panel_reveal_amount) * default_inset);
+    const int leading_inset =
+        leading_vertical_tab_strip_attached
+            ? 0
+            : (include_leading_inset ? default_inset : panel_side_inset);
+    const int trailing_inset =
+        trailing_vertical_tab_strip_attached
+            ? 0
+            : (include_trailing_inset ? default_inset : panel_side_inset);
 
     const gfx::Insets contents_insets = gfx::Insets::TLBR(
-        include_top_inset ? default_inset : 0,
-        include_leading_inset ? default_inset : panel_side_inset, default_inset,
-        include_trailing_inset ? default_inset : panel_side_inset);
+        top_inset, leading_inset, default_inset, trailing_inset);
 
     views().multi_contents_view->SetSplitViewInsets(contents_insets);
   }
@@ -1270,6 +1426,8 @@ BrowserViewTabbedLayoutImpl::CalculatePr
 
     views().vertical_tab_strip_region_view->SetHasLeadingExclusionForLayout(
         has_leading_exclusion);
+    views().vertical_tab_strip_region_view->SetZenModeFloatingStyleForLayout(
+        zen.floating_vertical_tab_strip);
   }
 
   return layout;
@@ -1284,6 +1442,8 @@ gfx::Rect BrowserViewTabbedLayoutImpl::C
       IsDynamicTabStrip(layout_data_->tab_strip_type);
   const bool show_dynamic_tab_strip =
       ShowDynamicTabStrip(layout_data_->tab_strip_type);
+  const ZenModeLayoutState zen = GetZenModeLayoutState(
+      delegate(), layout_data_->tab_strip_type == TabStripType::kVertical);
 
   if (IsParentedTo(views().horizontal_tab_strip_region_view,
                    views().top_container)) {
@@ -1292,12 +1452,19 @@ gfx::Rect BrowserViewTabbedLayoutImpl::C
       const int leading_margin = GetHorizontalTabStripLeadingMargin(params);
       tabstrip_bounds = GetBoundsWithExclusion(
           params, views().horizontal_tab_strip_region_view, leading_margin);
-      params.SetTop(tabstrip_bounds.bottom() -
-                    GetLayoutConstant(LayoutConstant::kTabstripToolbarOverlap));
+      tabstrip_bounds =
+          SlideBoundsFromTop(tabstrip_bounds, zen.top_chrome_ratio);
+      if (tabstrip_bounds.height() > 0) {
+        params.SetTop(tabstrip_bounds.bottom() -
+                      ScaleExtent(GetLayoutConstant(
+                                      LayoutConstant::kTabstripToolbarOverlap),
+                                  zen.top_chrome_ratio));
+      }
       needs_exclusion = false;
     }
     layout.AddChild(views().horizontal_tab_strip_region_view, tabstrip_bounds,
-                    layout_data_->tab_strip_type == TabStripType::kHorizontal);
+                    layout_data_->tab_strip_type == TabStripType::kHorizontal &&
+                        tabstrip_bounds.height() > 0);
   }
 
   // Lay out toolbar. If toolbar is in the top-most row, it will go into the
@@ -1318,21 +1485,26 @@ gfx::Rect BrowserViewTabbedLayoutImpl::C
                           params.visual_client_area.y(),
                           params.visual_client_area.width(),
                           views().toolbar->GetPreferredSize().height());
+      toolbar_bounds =
+          SlideBoundsFromTop(toolbar_bounds, zen.top_chrome_ratio);
       params.SetTop(toolbar_bounds.bottom());
       needs_exclusion = false;
     }
-    layout.AddChild(views().toolbar, toolbar_bounds, toolbar_visible);
+    layout.AddChild(views().toolbar, toolbar_bounds,
+                    toolbar_visible && toolbar_bounds.height() > 0);
   }
 
   // Clamp the margins visually by overlapping toolbar and the tab strip
-  if (dynamic_tab_strip && show_dynamic_tab_strip) {
+  if (dynamic_tab_strip && show_dynamic_tab_strip && zen.TopChromeVisible()) {
     const int toolbar_tabstrip_overlap =
         toolbar_visible ? GetLayoutConstant(LayoutConstant::kHeliumBasePadding)
                         : 0;
     params.SetTop(
         params.visual_client_area.y() +
-        views().horizontal_tab_strip_region_view->GetPreferredSize().height() -
-        toolbar_tabstrip_overlap);
+        ScaleExtent(views().horizontal_tab_strip_region_view->GetPreferredSize()
+                            .height() -
+                        toolbar_tabstrip_overlap,
+                    zen.top_chrome_ratio));
   }
 
   // Lay out the bookmarks bar if one is present.
@@ -1341,24 +1513,33 @@ gfx::Rect BrowserViewTabbedLayoutImpl::C
     const gfx::Rect bookmarks_bounds(
         params.visual_client_area.x(), params.visual_client_area.y(),
         params.visual_client_area.width(),
-        bookmarks_visible ? views().bookmark_bar->GetPreferredSize().height()
-                          : 0);
-    layout.AddChild(views().bookmark_bar, bookmarks_bounds, bookmarks_visible);
+        bookmarks_visible && zen.TopChromeVisible()
+            ? ScaleExtent(views().bookmark_bar->GetPreferredSize().height(),
+                          zen.top_chrome_ratio)
+            : 0);
+    layout.AddChild(views().bookmark_bar, bookmarks_bounds,
+                    bookmarks_visible && zen.TopChromeVisible() &&
+                        bookmarks_bounds.height() > 0);
     params.SetTop(bookmarks_bounds.bottom());
   }
 
   // Maybe show the separator in the top container.
   if (IsParentedTo(views().top_container_separator, views().top_container)) {
     gfx::Rect separator_bounds;
-    if (layout_data_->separator_info.top_container_separator) {
+    if (layout_data_->separator_info.top_container_separator &&
+        zen.TopChromeVisible()) {
+      const int separator_height = ScaleExtent(
+          views().top_container_separator->GetPreferredSize().height(),
+          zen.top_chrome_ratio);
       separator_bounds = gfx::Rect(
           params.visual_client_area.x(), params.visual_client_area.y(),
-          params.visual_client_area.width(),
-          views().top_container_separator->GetPreferredSize().height());
+          params.visual_client_area.width(), separator_height);
       params.SetTop(separator_bounds.bottom());
     }
     layout.AddChild(views().top_container_separator, separator_bounds,
-                    layout_data_->separator_info.top_container_separator);
+                    layout_data_->separator_info.top_container_separator &&
+                        zen.TopChromeVisible() &&
+                        separator_bounds.height() > 0);
   }
 
   return gfx::Rect(params.visual_client_area.x(), original_top,
@@ -1366,13 +1547,60 @@ gfx::Rect BrowserViewTabbedLayoutImpl::C
                    params.visual_client_area.y() - original_top);
 }
 
+void BrowserViewTabbedLayoutImpl::AdjustTopContainerForZenMode(
+    ProposedLayout& top_container_layout,
+    BrowserLayoutParams& params,
+    float top_chrome_ratio) const {
+#if BUILDFLAG(IS_LINUX)
+  constexpr int kZenModeTopBorderHeight = 4;
+  if (top_chrome_ratio >= 1.0f || views().browser_view->bounds().y() > 0) {
+    return;
+  }
+  if (top_container_layout.bounds.height() < kZenModeTopBorderHeight) {
+    top_container_layout.bounds.set_height(kZenModeTopBorderHeight);
+    params.SetTop(top_container_layout.bounds.bottom());
+  }
+#endif  // BUILDFLAG(IS_LINUX)
+}
+
 void BrowserViewTabbedLayoutImpl::ConfigureTopContainerBackground(
     const BrowserLayoutParams& params,
     CustomCornersBackground* background) {
   CHECK(layout_data_);
-  // The top container always draws an opaque background in tabbed browser mode
-  // to avoid cracking between visual elements.
-  background->SetVisible(true);
+  const bool frame_has_top_border = views().browser_view->bounds().y() > 0;
+  const bool zen_mode = delegate().IsZenModeEnabledForLayout();
+  const bool zen_hiding =
+      zen_mode && delegate().GetTopChromeRevealRatio() < 1.0f;
+
+  if (zen_hiding && frame_has_top_border) {
+    background->SetVisible(false);
+    background->SetCorners({});
+  } else {
+    background->SetVisible(true);
+  }
+
+  if (zen_mode && !frame_has_top_border) {
+    if (zen_hiding) {
+      background->SetPrimaryColor(CustomCornersBackground::FrameTheme());
+    } else {
+      background->SetPrimaryColor(CustomCornersBackground::ToolbarTheme());
+    }
+    const auto window_corner = background->GetWindowCorner(/*upper=*/true);
+    CustomCornersBackground::Corners corners;
+    if (delegate().IsZenModeSideChromePinned()) {
+      const bool vts_right = delegate().IsVerticalTabStripRightAligned();
+      if (vts_right) {
+        corners[CornerOrientation::kTopLeading] = window_corner;
+      } else {
+        corners[CornerOrientation::kTopTrailing] = window_corner;
+      }
+    } else {
+      corners[CornerOrientation::kTopLeading] = window_corner;
+      corners[CornerOrientation::kTopTrailing] = window_corner;
+    }
+    background->SetCorners(corners);
+    return;
+  }
 
   // In immersive mode and ChromeOS touch UI mode, the top Chrome UI is
   // parented to the `top_container()` and the frame header is not visible.
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -1706,7 +1706,7 @@ void ToolbarView::OnThemeChanged() {
 }
 
 void ToolbarView::NewTabButtonPressed(const ui::Event& event) {
-  chrome::NewTab(browser_);
+  chrome::NewTab(browser_, NewTabTypes::kNewTabButton);
 }
 
 void ToolbarView::VerticalTabsCollapseButtonPressed(const ui::Event& event) {
@@ -2070,6 +2070,16 @@ void ToolbarView::UpdateToolbarLayout()
   InvalidateLayout();
 }
 
+void ToolbarView::AbortToolbarContainerAnimationsForZenReveal() {
+  if (extensions_container_) {
+    extensions_container_->GetAnimatingLayoutManager()->ResetLayout();
+  }
+  if (pinned_toolbar_actions_container_) {
+    pinned_toolbar_actions_container_->GetAnimatingLayoutManager()
+        ->ResetLayout();
+  }
+}
+
 bool ToolbarView::IsCompactLayout() const {
   auto* controller =
       HeliumLayoutStateController::From(browser_view_->browser());
@@ -2107,8 +2117,11 @@ void ToolbarView::UpdateVerticalTabsColl
 
   auto* controller =
       tabs::VerticalTabStripStateController::From(browser_view_->browser());
+  const bool zen_hide = browser_view_->IsZenModeEnabledForLayout() &&
+                        !browser_view_->IsZenModeSideChromePinned();
   const bool should_show = controller && IsVerticalLayout() &&
-                           show_vertical_tabs_collapse_button_.GetValue();
+                           show_vertical_tabs_collapse_button_.GetValue() &&
+                           !zen_hide;
   vertical_tabs_collapse_button_->SetVisible(should_show);
 
   if (!should_show) {
--- a/chrome/browser/ui/views/toolbar/toolbar_view.h
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.h
@@ -180,6 +180,13 @@ class ToolbarView : public views::Access
   // Attaches the tab strip region view when compact layout is enabled.
   void AttachTabStripRegionView(TabStripRegionView* view);
 
+  // Resets extension-related toolbar container animations so they don't animate
+  // while zen mode is revealing the toolbar.
+  void AbortToolbarContainerAnimationsForZenReveal();
+
+  // Updates the vertical tabs collapse button visibility and icon.
+  void UpdateVerticalTabsCollapseButton();
+
   // Accessors.
   Browser* browser() const { return browser_; }
   views::Button* GetChromeLabsButton() const;
@@ -303,7 +310,6 @@ class ToolbarView : public views::Access
   bool IsVerticalLayout() const;
   bool IsVerticalTabsRightAligned() const;
   bool ShouldShowToolbarNewTabButton() const;
-  void UpdateVerticalTabsCollapseButton();
   void UpdateDynamicNewTabButton();
 
   // AppMenuIconController::Delegate:
--- a/chrome/browser/ui/browser_command_controller.cc
+++ b/chrome/browser/ui/browser_command_controller.cc
@@ -1495,7 +1495,17 @@ void BrowserCommandController::HandleCom
       break;
     }
     case IDC_CTRL_S_SHORTCUT: {
+      // In zen mode with VTS, Ctrl+S toggles the side chrome visibility
+      // instead of collapsing the tab strip.
       PrefService* prefs = profile()->GetPrefs();
+      auto* browser_view = BrowserView::GetBrowserViewForBrowser(browser_);
+      if (browser_view && browser_view->IsZenModeEnabledForLayout() &&
+          browser_view->ShouldDrawVerticalTabStrip() &&
+          ShouldToggleVerticalCollapse(prefs)) {
+        browser_view->ToggleZenModeSideChrome();
+        break;
+      }
+
       if (ShouldToggleVerticalCollapse(prefs)) {
         auto* controller =
             tabs::VerticalTabStripStateController::From(browser_);
@@ -1509,6 +1519,13 @@ void BrowserCommandController::HandleCom
                                     time_stamp);
       break;
     }
+    case IDC_TOGGLE_ZEN_MODE_TOP_CHROME_PIN: {
+      if (auto* browser_view =
+              BrowserView::GetBrowserViewForBrowser(browser_)) {
+        browser_view->ToggleZenModeTopChrome();
+      }
+      break;
+    }
     // Hosted App commands
     case IDC_COPY_URL:
       CopyURL(browser_, browser_->tab_strip_model()->GetActiveWebContents());
--- a/chrome/browser/ui/views/frame/multi_contents_view.h
+++ b/chrome/browser/ui/views/frame/multi_contents_view.h
@@ -88,6 +88,9 @@ class MultiContentsView
   void SetBackgroundRadii(const gfx::RoundedCornersF& radii);
   const gfx::RoundedCornersF& GetBackgroundRadii() const;
 
+  void MaybeUpdateContentsBorderAndOverlay();
+  void MaybeUpdateBorderForZenReveal();
+
   // Returns true if more than one WebContents is displayed.
   bool IsInSplitView() const;
 
@@ -239,6 +242,14 @@ class MultiContentsView
     bool trailing_side_chrome_attached = false;
   };
 
+  struct AttachedChromeEdges {
+    bool top = false;
+    bool leading = false;
+    bool trailing = false;
+
+    int TopInset() const;
+  };
+
   static constexpr int kMinWebContentsSize = 200;
   static constexpr int kConstrainedMinWebContentsSize = 50;
   static constexpr double kMinWebContentsSizePercentage = 0.1;
@@ -252,6 +263,7 @@ class MultiContentsView
   // available space after the layout.
   gfx::Rect CalculateDropTargetLayout(
       const gfx::Rect& available_space,
+      const AttachedChromeEdges& attached_chrome,
       std::vector<views::ChildLayout>& child_layouts) const;
 
   // Adds separator layouts to the given list and returns the remaining
@@ -279,6 +291,9 @@ class MultiContentsView
   // `MultiContentsView`. Returns 0 if not in a split view.
   int GetMinViewSize(gfx::Rect available_space) const;
 
+  AttachedChromeEdges GetAttachedChromeEdges() const;
+  bool IsTopChromeAttachedForBorder() const;
+
   void UpdateContentsBorderAndOverlay() const;
 
   double CalculateRatioWithSnapPoints(double start_size,
@@ -336,10 +351,11 @@ class MultiContentsView
   // a resize action began. Nullopt if not currently resizing.
   std::optional<int> initial_start_size_on_resize_;
 
-  // Insets of the start and end contents view when in split view.
+  // Insets of the contents view when in split view.
   gfx::Insets split_view_insets_;
 
   bool active_contents_view_highlighted_ = false;
+  std::optional<bool> top_chrome_attached_for_border_;
 
   std::optional<int> min_contents_size_for_testing_;
 
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.h
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.h
@@ -49,6 +49,12 @@ class BrowserViewTabbedLayoutImpl : publ
   void DoPostLayoutCleanup() override;
 
  private:
+  // When zen mode hides chrome, reserves a border strip in the top container
+  // so the background can paint the window's top border.
+  void AdjustTopContainerForZenMode(ProposedLayout& top_container_layout,
+                                    BrowserLayoutParams& params,
+                                    float top_chrome_ratio) const;
+
   struct HorizontalLayout;
   struct SeparatorInfo;
   struct TransientLayoutData;
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
@@ -38,6 +38,7 @@
 #include "chrome/browser/ui/views/frame/custom_corners_background.h"
 #include "chrome/browser/ui/views/frame/shadow_frame_view.h"
 #include "chrome/browser/ui/views/frame/tab_strip_region_view.h"
+#include "chrome/browser/ui/views/helium/frame_corner_radius.h"
 #include "chrome/browser/ui/views/tabs/common/pinned_tab_container_view.h"
 #include "chrome/browser/ui/views/tabs/common/root_tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/common/tab_collection_node.h"
@@ -294,6 +295,44 @@ void VerticalTabStripRegionView::SetHasL
   UpdateInteriorMargin();
 }
 
+void VerticalTabStripRegionView::SetZenModeFloatingStyleForLayout(
+    bool floating) {
+  if (zen_mode_floating_style_ == floating) {
+    return;
+  }
+  zen_mode_floating_style_ = floating;
+  UpdateResizeAreaVisibility();
+  SetClipLayerToVisibleBounds(floating);
+  UpdateInteriorMargin();
+
+  auto* const background =
+      static_cast<CustomCornersBackground*>(this->background());
+  if (!background) {
+    return;
+  }
+
+  CustomCornersBackground::Corners corners;
+  CustomCornersBackground::Outline outline;
+  if (floating) {
+    CustomCornersBackground::Corner rounded_corner;
+    rounded_corner.type = CustomCornersBackground::CornerType::kRounded;
+    rounded_corner.radius = GetGenericFrameRadius();
+    corners[CornerOrientation::kTopLeading] = rounded_corner;
+    corners[CornerOrientation::kTopTrailing] = rounded_corner;
+    corners[CornerOrientation::kBottomLeading] = rounded_corner;
+    corners[CornerOrientation::kBottomTrailing] = rounded_corner;
+
+    outline.color.color = kColorToolbarContentAreaSeparator;
+    outline.top = true;
+    outline.leading = true;
+    outline.bottom = true;
+    outline.trailing = true;
+  }
+
+  background->SetCorners(corners);
+  background->SetOutline(outline);
+}
+
 void VerticalTabStripRegionView::AddedToWidget() {
   BaseTabStripRegionView::AddedToWidget();
   paint_as_active_subscription_ =
@@ -591,8 +630,7 @@ void VerticalTabStripRegionView::OnResiz
   }
 
   if (done_resizing) {
-    resize_area_->SetVisible(!state_controller_->IsCollapsed() ||
-                             !state_controller_->IsExpandOnHoverEnabled());
+    UpdateResizeAreaVisibility();
     base::RecordAction(base::UserMetricsAction(
         new_state.collapsed ? "VerticalTabs_TabStrip_ResizeToCollapsed"
                             : "VerticalTabs_TabStrip_ResizeToUncollapsed"));
@@ -721,7 +759,22 @@ void VerticalTabStripRegionView::OnTabSt
 void VerticalTabStripRegionView::UpdateInteriorMargin() {
   const int padding = GetLayoutConstant(
       LayoutConstant::kVerticalTabStripUncollapsedVerticalPadding);
-  flex_layout_->SetInteriorMargin(gfx::Insets::TLBR(0, 0, padding, 0));
+  int top_padding = 0;
+  BrowserView* const browser = browser_view();
+  if (browser && browser->IsZenModeEnabledForLayout()) {
+    const float top_chrome_ratio = browser->GetTopChromeRevealRatio();
+    const bool top_chrome_visible_or_pinned =
+        top_chrome_ratio > 0.0f || browser->IsZenModeTopChromePinned();
+    const float ratio =
+        zen_mode_floating_style_ || !top_chrome_visible_or_pinned
+            ? 1.0f
+            : 1.0f - top_chrome_ratio;
+    top_padding = base::ClampRound(
+        GetLayoutConstant(LayoutConstant::kVerticalTabCornerRadius) * ratio);
+  }
+
+  flex_layout_->SetInteriorMargin(
+      gfx::Insets::TLBR(top_padding, 0, padding, 0));
 }
 
 void VerticalTabStripRegionView::OnCollapseStateChanged(
@@ -730,9 +783,7 @@ void VerticalTabStripRegionView::OnColla
   // the collapsing state.
   bool collapsed = state != tabs::VerticalTabStripCollapseState::kExpanded;
 
-  resize_area_->SetVisible(!collapsed ||
-                           !state_controller_->IsExpandOnHoverEnabled() ||
-                           resize_area_->is_resizing());
+  UpdateResizeAreaVisibility();
 
   resize_area_width_ = collapsed ? kCollapsedResizeAreaWidth : kResizeAreaWidth;
 
@@ -746,6 +797,22 @@ void VerticalTabStripRegionView::OnColla
   }
 }
 
+void VerticalTabStripRegionView::UpdateResizeAreaVisibility() {
+  if (zen_mode_floating_style_) {
+    resize_area_->SetVisible(false);
+    return;
+  }
+
+  const bool is_fully_expanded =
+      state_controller_->GetCollapseState() ==
+      tabs::VerticalTabStripCollapseState::kExpanded;
+  const bool can_resize_collapsed_tab_strip =
+      !state_controller_->IsExpandOnHoverEnabled() ||
+      resize_area_->is_resizing();
+
+  resize_area_->SetVisible(is_fully_expanded || can_resize_collapsed_tab_strip);
+}
+
 void VerticalTabStripRegionView::UpdateColors() {
   SchedulePaint();
 }
@@ -773,9 +840,9 @@ gfx::Rect VerticalTabStripRegionView::Ge
   return tab_strip_draggable_bounds;
 }
 
-void VerticalTabStripRegionView::OnExpandOnHoverEnabledChanged(bool enabled) {
-  resize_area_->SetVisible(!state_controller_->IsCollapsed() || !enabled ||
-                           resize_area_->is_resizing());
+void VerticalTabStripRegionView::OnExpandOnHoverEnabledChanged(
+    bool /*enabled*/) {
+  UpdateResizeAreaVisibility();
   UpdateExpandOnHoverState();
 }
 
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
@@ -86,6 +86,9 @@ class VerticalTabStripRegionView final
   bool IsPositionInWindowCaption(const gfx::Point& point) override;
 
   void SetHasLeadingExclusionForLayout(bool has_leading_exclusion);
+  void SetZenModeFloatingStyleForLayout(bool floating);
+
+  void UpdateInteriorMargin();
 
   // views::View:
   void AddedToWidget() override;
@@ -189,10 +192,9 @@ class VerticalTabStripRegionView final
   void OnTabStripViewSet() override;
   void OnTabStripViewWillClear() override;
 
-  void UpdateInteriorMargin();
-
   void OnCollapseStateChanged(
       tabs::VerticalTabStripCollapseState collapse_state);
+  void UpdateResizeAreaVisibility();
 
   void UpdateColors();
 
@@ -231,6 +233,7 @@ class VerticalTabStripRegionView final
 
   // Whether a leading exclusion exists due to window controls.
   bool has_leading_exclusion_ = false;
+  bool zen_mode_floating_style_ = false;
 
   raw_ptr<VerticalTabStripBottomContainer> bottom_button_container_ = nullptr;
   raw_ptr<views::View> gemini_button_ = nullptr;
--- a/chrome/browser/ui/views/frame/contents_container_view.cc
+++ b/chrome/browser/ui/views/frame/contents_container_view.cc
@@ -197,16 +197,22 @@ void ContentsContainerView::UpdateBorder
                                                    bool should_round_contents,
                                                    bool is_active,
                                                    bool is_highlighted,
+                                                   bool round_top_left,
+                                                   bool round_top_right,
                                                    bool round_bottom_left,
                                                    bool round_bottom_right) {
   const bool split_changed = is_in_split != is_in_split_;
   const bool rounded_contents_changed =
       should_round_contents != should_round_contents_;
-  const bool bottom_corner_state_changed =
+  const bool corner_state_changed =
+      round_top_left_window_corner_ != round_top_left ||
+      round_top_right_window_corner_ != round_top_right ||
       round_bottom_left_window_corner_ != round_bottom_left ||
       round_bottom_right_window_corner_ != round_bottom_right;
   is_in_split_ = is_in_split;
   should_round_contents_ = should_round_contents;
+  round_top_left_window_corner_ = round_top_left;
+  round_top_right_window_corner_ = round_top_right;
   round_bottom_left_window_corner_ = round_bottom_left;
   round_bottom_right_window_corner_ = round_bottom_right;
 
@@ -231,8 +237,7 @@ void ContentsContainerView::UpdateBorder
             gfx::Insets(ContentsContainerOutline::kThickness)));
       }
     }
-    if (split_changed || rounded_contents_changed ||
-        bottom_corner_state_changed) {
+    if (split_changed || rounded_contents_changed || corner_state_changed) {
       UpdateBorderRoundedCorners();
     }
 
@@ -340,13 +345,18 @@ void ContentsContainerView::UpdateBorder
       round_bottom_left_window_corner_ ? frame_radius : generic_radius;
   const int lower_right_radius =
       round_bottom_right_window_corner_ ? frame_radius : generic_radius;
+  const int upper_left_radius =
+      round_top_left_window_corner_ ? frame_radius : generic_radius;
+  const int upper_right_radius =
+      round_top_right_window_corner_ ? frame_radius : generic_radius;
 
   const gfx::RoundedCornersF container_rounded_corners(
-      generic_radius, generic_radius, lower_right_radius, lower_left_radius);
+      upper_left_radius, upper_right_radius, lower_right_radius,
+      lower_left_radius);
   const int inner_padding = is_in_split_ ? kSplitViewContentPadding
                                          : ContentsContainerOutline::kThickness;
   const gfx::RoundedCornersF inner_rounded_corners(
-      generic_radius - inner_padding, generic_radius - inner_padding,
+      upper_left_radius - inner_padding, upper_right_radius - inner_padding,
       lower_right_radius - inner_padding, lower_left_radius - inner_padding);
 
   container_outline_->SetCornerRadii(container_rounded_corners);
--- a/chrome/browser/ui/views/frame/contents_container_view.h
+++ b/chrome/browser/ui/views/frame/contents_container_view.h
@@ -120,6 +120,8 @@ class ContentsContainerView : public vie
                               bool should_round_contents,
                               bool is_active,
                               bool is_highlighted,
+                              bool round_top_left_window_corner,
+                              bool round_top_right_window_corner,
                               bool round_bottom_left_window_corner,
                               bool round_bottom_right_window_corner);
 
@@ -168,6 +170,8 @@ class ContentsContainerView : public vie
 
   bool is_in_split_ = false;
   bool should_round_contents_ = false;
+  bool round_top_left_window_corner_ = false;
+  bool round_top_right_window_corner_ = false;
   bool round_bottom_left_window_corner_ = false;
   bool round_bottom_right_window_corner_ = false;
 
--- a/chrome/browser/ui/views/frame/multi_contents_view.cc
+++ b/chrome/browser/ui/views/frame/multi_contents_view.cc
@@ -6,6 +6,7 @@
 
 #include <algorithm>
 #include <cstdlib>
+#include <utility>
 
 #include "base/check_deref.h"
 #include "base/notreached.h"
@@ -49,8 +50,18 @@
 
 namespace {
 constexpr int kSnapDistance = 15;
+constexpr float kZenTopChromeAttachedThreshold = 0.5f;
+
+bool IsTopChromeAttached(const BrowserView& browser_view,
+                         bool should_show_top_separator) {
+  return should_show_top_separator &&
+         (!browser_view.IsZenModeEnabledForLayout() ||
+          browser_view.GetTopChromeRevealRatio() >=
+              kZenTopChromeAttachedThreshold);
 }
 
+}  // namespace
+
 void MultiContentsView::ContentsSeparators::Reset() {
   top_separator = nullptr;
   leading_separator = nullptr;
@@ -444,6 +455,41 @@ int MultiContentsView::GetInactiveIndex(
   return active_index_ == 0 ? 1 : 0;
 }
 
+void MultiContentsView::MaybeUpdateContentsBorderAndOverlay() {
+  top_chrome_attached_for_border_ = IsTopChromeAttachedForBorder();
+  if (IsInSplitView() || ShouldUseRoundedFrame()) {
+    UpdateContentsBorderAndOverlay();
+  }
+}
+
+void MultiContentsView::MaybeUpdateBorderForZenReveal() {
+  const bool top_chrome_attached = IsTopChromeAttachedForBorder();
+  if (top_chrome_attached_for_border_ &&
+      *top_chrome_attached_for_border_ == top_chrome_attached) {
+    return;
+  }
+  top_chrome_attached_for_border_ = top_chrome_attached;
+  if (IsInSplitView() || ShouldUseRoundedFrame()) {
+    UpdateContentsBorderAndOverlay();
+  }
+}
+
+int MultiContentsView::AttachedChromeEdges::TopInset() const {
+  return top ? 0 : MultiContentsView::kSplitViewContentInset;
+}
+
+MultiContentsView::AttachedChromeEdges
+MultiContentsView::GetAttachedChromeEdges() const {
+  const bool split_view = IsInSplitView();
+  AttachedChromeEdges edges;
+  edges.top = IsTopChromeAttachedForBorder();
+  edges.leading = contents_separators_.leading_side_chrome_attached ||
+                  (split_view && split_view_insets_.left() == 0);
+  edges.trailing = contents_separators_.trailing_side_chrome_attached ||
+                   (split_view && split_view_insets_.right() == 0);
+  return edges;
+}
+
 void MultiContentsView::OnWebContentsFocused(views::WebView* web_view) {
   if (IsInSplitView()) {
     // Check whether the widget is visible as otherwise during browser hide,
@@ -506,15 +552,17 @@ views::ProposedLayout MultiContentsView:
 
   gfx::Rect available_space = gfx::Rect(width, height);
 
+  const bool is_in_split = IsInSplitView();
   const bool should_use_rounded_frame = ShouldUseRoundedFrame();
-  const bool show_background = drop_target_view_->GetVisible() ||
-                               IsInSplitView() || should_use_rounded_frame;
+  const AttachedChromeEdges attached_chrome = GetAttachedChromeEdges();
+  const bool show_background = drop_target_view_->GetVisible() || is_in_split ||
+                               should_use_rounded_frame;
   layouts.child_layouts.emplace_back(background_view_.get(), show_background,
                                      available_space);
 
   if (IsDragAndDropEnabled()) {
-    available_space =
-        CalculateDropTargetLayout(available_space, layouts.child_layouts);
+    available_space = CalculateDropTargetLayout(
+        available_space, attached_chrome, layouts.child_layouts);
   }
 
   available_space =
@@ -524,8 +572,7 @@ views::ProposedLayout MultiContentsView:
     available_space.Inset(split_view_insets_);
   } else if (should_use_rounded_frame) {
     gfx::Insets single_insets(kSplitViewContentInset);
-    single_insets.set_top(
-        contents_separators_.should_show_top ? 0 : kSplitViewContentInset);
+    single_insets.set_top(attached_chrome.TopInset());
     single_insets.set_left(contents_separators_.leading_side_chrome_attached
                                ? 0
                                : kSplitViewContentInset);
@@ -632,6 +679,7 @@ void MultiContentsView::BeforeApplyLayou
 
 gfx::Rect MultiContentsView::CalculateDropTargetLayout(
     const gfx::Rect& available_space,
+    const AttachedChromeEdges& attached_chrome,
     std::vector<views::ChildLayout>& child_layouts) const {
   CHECK(IsDragAndDropEnabled());
   if (!drop_target_view_->GetVisible()) {
@@ -649,13 +697,12 @@ gfx::Rect MultiContentsView::CalculateDr
   const bool is_drop_target_on_bottom =
       drop_side == MultiContentsDropTargetView::DropSide::BOTTOM;
   const bool should_use_rounded_frame = ShouldUseRoundedFrame();
+  const bool zen_mode = browser_view_->IsZenModeEnabledForLayout();
   bool drop_target_side_chrome_attached = false;
   if (is_drop_target_on_start) {
-    drop_target_side_chrome_attached =
-        contents_separators_.leading_side_chrome_attached;
+    drop_target_side_chrome_attached = attached_chrome.leading;
   } else if (is_drop_target_on_end) {
-    drop_target_side_chrome_attached =
-        contents_separators_.trailing_side_chrome_attached;
+    drop_target_side_chrome_attached = attached_chrome.trailing;
   }
   const views::Widget* widget = browser_view_->GetWidget();
   const bool use_system_side_corners =
@@ -664,8 +711,8 @@ gfx::Rect MultiContentsView::CalculateDr
       drop_target_side_chrome_attached,
       drop_target_side_chrome_attached || !should_use_rounded_frame,
       !is_drop_target_on_bottom && use_system_side_corners &&
-          !contents_separators_.should_show_top &&
-          !should_use_rounded_frame,
+          !attached_chrome.top &&
+          (!should_use_rounded_frame || zen_mode),
       use_system_side_corners);
   UpdateContentsBorderAndOverlay();
 
@@ -692,10 +739,8 @@ gfx::Rect MultiContentsView::CalculateDr
 
   if (!is_drop_target_on_bottom) {
     int top_margin = 0;
-    if (IsInSplitView() || should_use_rounded_frame) {
-      top_margin = contents_separators_.should_show_top
-                       ? 0
-                       : kSplitViewContentInset;
+    if (IsInSplitView() || should_use_rounded_frame || zen_mode) {
+      top_margin = attached_chrome.TopInset();
     } else if (contents_separators_.should_show_top) {
       top_margin =
           contents_separators_.top_separator->GetPreferredSize().height();
@@ -832,6 +877,12 @@ bool MultiContentsView::ShouldUseRounded
          !is_fullscreen;
 }
 
+bool MultiContentsView::IsTopChromeAttachedForBorder() const {
+  return top_edge_attached_to_infobar_ ||
+         IsTopChromeAttached(*browser_view_,
+                             contents_separators_.should_show_top);
+}
+
 void MultiContentsView::UpdateContentsBorderAndOverlay() const {
   const views::Widget* widget = browser_view_->GetWidget();
   const bool is_fullscreen = widget && widget->IsFullscreen();
@@ -848,18 +899,26 @@ void MultiContentsView::UpdateContentsBo
   const bool is_drop_target_on_bottom =
       is_drop_target_visible &&
       drop_target_view_->side() == MultiContentsDropTargetView::DropSide::BOTTOM;
-  const bool should_show_leading =
-      contents_separators_.leading_side_chrome_attached;
-  const bool should_show_trailing =
-      contents_separators_.trailing_side_chrome_attached;
-
+  const AttachedChromeEdges attached_chrome = GetAttachedChromeEdges();
+  const bool top_edge_is_window_edge = !attached_chrome.top;
+  const bool can_use_system_corners =
+      (is_in_split || should_round_single_contents) && !is_fullscreen;
+  const bool leading_edge_is_window_edge =
+      !attached_chrome.leading && !is_drop_target_on_start;
+  const bool trailing_edge_is_window_edge =
+      !attached_chrome.trailing && !is_drop_target_on_end;
+
+  const bool use_system_top_left_radius = can_use_system_corners &&
+                                          top_edge_is_window_edge &&
+                                          leading_edge_is_window_edge;
+  const bool use_system_top_right_radius = can_use_system_corners &&
+                                           top_edge_is_window_edge &&
+                                           trailing_edge_is_window_edge;
   const bool use_system_bottom_left_radius =
-      (is_in_split || should_round_single_contents) && !is_fullscreen &&
-      !should_show_leading && !is_drop_target_on_start &&
+      can_use_system_corners && leading_edge_is_window_edge &&
       !is_drop_target_on_bottom;
   const bool use_system_bottom_right_radius =
-      (is_in_split || should_round_single_contents) && !is_fullscreen &&
-      !should_show_trailing && !is_drop_target_on_end &&
+      can_use_system_corners && trailing_edge_is_window_edge &&
       !is_drop_target_on_bottom;
 
   for (auto& contents_container_view : contents_container_views_) {
@@ -883,6 +942,8 @@ void MultiContentsView::UpdateContentsBo
     contents_container_view->UpdateBorderAndOverlay(
         is_in_split, should_round_contents, is_active,
         is_active && active_contents_view_highlighted_,
+        use_system_top_left_radius && is_first_view,
+        use_system_top_right_radius && is_last_view,
         use_system_bottom_left_radius && is_first_view,
         use_system_bottom_right_radius && is_last_view);
   }
@@ -944,9 +1005,7 @@ void MultiContentsView::SetTopChromeAtta
   }
   top_edge_attached_to_infobar_ = top_edge_attached_to_infobar;
   contents_separators_.should_show_top = should_show_top_separator;
-  if (IsInSplitView() || ShouldUseRoundedFrame()) {
-    UpdateContentsBorderAndOverlay();
-  }
+  MaybeUpdateContentsBorderAndOverlay();
   // This can be called during BrowserView layout, so protect against creating a
   // layout loop.
   InvalidateLayout(/*avoid_propagate_during_layout=*/true);
@@ -961,11 +1020,7 @@ void MultiContentsView::SetAttachedSideC
   }
   contents_separators_.leading_side_chrome_attached = leading_attached;
   contents_separators_.trailing_side_chrome_attached = trailing_attached;
-  if (IsInSplitView() || ShouldUseRoundedFrame()) {
-    UpdateContentsBorderAndOverlay();
-  }
-  // This can be called during BrowserView layout, so protect against creating a
-  // layout loop.
+  MaybeUpdateContentsBorderAndOverlay();
   InvalidateLayout(/*avoid_propagate_during_layout=*/true);
 }
 
--- a/chrome/browser/ui/views/location_bar/location_bar_view.cc
+++ b/chrome/browser/ui/views/location_bar/location_bar_view.cc
@@ -294,6 +294,9 @@ void LocationBarView::Init() {
   // to draw outside the parent's bounds.
   SetPaintToLayer();
   layer()->SetFillsBoundsOpaquely(false);
+#if BUILDFLAG(IS_LINUX)
+  SetClipLayerToVisibleBounds(true);
+#endif
 
   if (base::FeatureList::IsEnabled(
           content_settings::features::kLeftHandSideActivityIndicators)) {
--- a/chrome/browser/ui/views/toolbar/toolbar_icon_container_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_icon_container_view.cc
@@ -11,6 +11,7 @@
 #include "base/memory/raw_ptr.h"
 #include "base/observer_list.h"
 #include "base/scoped_observation.h"
+#include "build/build_config.h"
 #include "chrome/browser/themes/theme_properties.h"
 #include "chrome/browser/ui/color/chrome_color_id.h"
 #include "chrome/browser/ui/layout_constants.h"
@@ -116,6 +117,9 @@ ToolbarIconContainerView::ToolbarIconCon
   SetPaintToLayer();
   layer()->SetFillsBoundsOpaquely(false);
   layer()->AsTextured()->SetFillsBoundsCompletely(false);
+#if BUILDFLAG(IS_LINUX)
+  SetClipLayerToVisibleBounds(true);
+#endif
   AddLayerToRegion(border_.layer(), views::LayerRegion::kBelow);
 
   views::AnimatingLayoutManager* animating_layout =
--- a/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
+++ b/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
@@ -400,8 +400,6 @@ views::ProposedLayout PinnedTabContainer
   // Since all children are allocated the same width this will be the same for
   // every row.
   if (size_bounds.width().is_bounded() && size_bounds.width().value() > 0) {
-    auto collapse_state = GetTabStripCollapseState();
-
     // Apply horizontal padding immediately at start of collapse animation by
     // including collapsing state.
     int available_width =
@@ -410,16 +408,12 @@ views::ProposedLayout PinnedTabContainer
 
     // When we are in collapsed state, only one child should be shown per row.
     // During collapse animation and other cases, fit as many as possible.
-    children_on_row =
-        tabs::IsVerticalTabsExpandOnHoverFeatureEnabled() &&
-                collapse_state ==
-                    tabs::VerticalTabStripCollapseState::kCollapsed
-            ? 1
-            : std::min(
-                  children_on_row,
-                  static_cast<int>(std::floor((available_width - child_width) /
-                                              (child_width + kTabPadding)) +
-                                   1));
+    children_on_row = std::max(
+        1,
+        std::min(children_on_row,
+                 static_cast<int>(std::floor((available_width - child_width) /
+                                             (child_width + kTabPadding)) +
+                                  1)));
 
     // Allocate extra space to the tabs.
     available_width -=
--- a/chrome/browser/ui/navigator/browser_navigator.cc
+++ b/chrome/browser/ui/navigator/browser_navigator.cc
@@ -32,9 +32,11 @@
 #include "chrome/browser/ui/browser_init_state.h"
 #include "chrome/browser/ui/browser_ui_controller/browser_ui_controller.h"
 #include "chrome/browser/ui/browser_window.h"
+#include "chrome/browser/ui/browser_window/public/browser_window_features.h"
 #include "chrome/browser/ui/browser_window/public/browser_window_interface.h"
 #include "chrome/browser/ui/browser_window/public/create_browser_window.h"
 #include "chrome/browser/ui/browser_window/public/profile_browser_collection.h"
+#include "chrome/browser/ui/helium/helium_layout_state_controller.h"
 #include "chrome/browser/ui/incognito_allowed_url.h"
 #include "chrome/browser/ui/location_bar/location_bar.h"
 #include "chrome/browser/ui/navigator/browser_navigator_params.h"
@@ -45,6 +47,9 @@
 #include "chrome/browser/ui/tabs/split_tab_metrics.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_user_gesture_details.h"
+#include "chrome/browser/ui/toasts/api/toast_id.h"
+#include "chrome/browser/ui/toasts/toast_controller.h"
+#include "chrome/browser/ui/views/frame/browser_view.h"
 #include "chrome/browser/ui/web_applications/app_browser_controller.h"
 #include "chrome/browser/ui/web_applications/navigation_capturing_process.h"
 #include "chrome/browser/ui/web_applications/web_app_launch_navigation_handle_user_data.h"
@@ -187,6 +192,64 @@ bool ShouldForceCaptivePortalSigninIntoC
 }
 #endif  // BUILDFLAG(IS_CHROMEOS)
 
+bool IsTabChromeRevealedInZenMode(
+    const HeliumLayoutStateController& layout_controller,
+    const NavigateParams& params) {
+  BrowserView* const browser_view =
+      BrowserView::GetBrowserViewForBrowser(params.browser);
+  if (!browser_view) {
+    return false;
+  }
+
+  // In vertical layout, tabs are in side chrome. In all other layouts,
+  // tabs are in top chrome.
+  if (layout_controller.IsVerticalLayout()) {
+    return browser_view->GetSideChromeRevealRatio() > 0.0f;
+  }
+  return browser_view->GetTopChromeRevealRatio() > 0.0f;
+}
+
+bool ShouldShowNewTabToastInZenMode(const NavigateParams& params) {
+  // Only show the toast when a new browser tab is created.
+  if (params.disposition != WindowOpenDisposition::NEW_FOREGROUND_TAB &&
+      params.disposition != WindowOpenDisposition::NEW_BACKGROUND_TAB) {
+    return false;
+  }
+
+  // Don't show a toast for omnibox commands, typed URLs, etc.
+  if (!ui::PageTransitionCoreTypeIs(params.transition,
+                                    ui::PAGE_TRANSITION_LINK) &&
+      !ui::PageTransitionCoreTypeIs(params.transition,
+                                    ui::PAGE_TRANSITION_AUTO_BOOKMARK)) {
+    return false;
+  }
+
+  // Zen Mode state is tracked per browser window.
+  if (!params.browser) {
+    return false;
+  }
+
+  // Don't show the toast if zen mode is disabled or if chrome with tabs
+  // is revealed. The user can already see the tab strip update in either
+  // of those cases.
+  auto* layout_controller = HeliumLayoutStateController::From(params.browser);
+  return layout_controller && layout_controller->IsZenModeEnabled() &&
+         !IsTabChromeRevealedInZenMode(*layout_controller, params);
+}
+
+void MaybeShowZenModeNewTabCreatedToast(const NavigateParams& params) {
+  if (!ShouldShowNewTabToastInZenMode(params)) {
+    return;
+  }
+
+  ToastController* const toast_controller =
+      params.browser->GetFeatures().toast_controller();
+  if (toast_controller) {
+    toast_controller->MaybeShowToast(
+        ToastParams(ToastId::kZenModeNewTabCreated));
+  }
+}
+
 // Change some of the navigation parameters based on the particular URL.
 // Returns true on success. Otherwise, if changing params leads the browser
 // into an erroneous state, returns false.
@@ -995,6 +1058,7 @@ base::WeakPtr<content::NavigationHandle>
     params->browser->GetBrowserForMigrationOnly()->tab_strip_model()->AddTab(
         std::move(tab_to_insert), params->tabstrip_index, params->transition,
         params->tabstrip_add_types, group);
+    MaybeShowZenModeNewTabCreatedToast(*params);
 
     // For NEW_SPLIT_VIEW, pair the new tab with the active tab. The
     // "already split" case is handled in Browser::OpenURLFromTab().
--- a/chrome/browser/ui/toasts/toast_service.cc
+++ b/chrome/browser/ui/toasts/toast_service.cc
@@ -90,6 +90,14 @@ void ToastService::RegisterToasts(
           .Build());
 
   toast_registry_->RegisterToast(
+      ToastId::kZenModeNewTabCreated,
+      ToastSpecification::Builder(features::IsRoundedIconsEnabled()
+                                      ? kTabIcon
+                                      : kNewTabRefreshOldIcon,
+                                  IDS_ZEN_MODE_NEW_TAB_CREATED_TOAST_BODY)
+          .Build());
+
+  toast_registry_->RegisterToast(
       ToastId::kImageCopied,
       ToastSpecification::Builder(features::IsRoundedIconsEnabled()
                                       ? vector_icons::kContentCopyIcon
--- a/chrome/browser/ui/views/side_panel/side_panel.cc
+++ b/chrome/browser/ui/views/side_panel/side_panel.cc
@@ -82,12 +82,15 @@ gfx::RoundedCornersF GetContentRoundedCo
       side_panel->GetWidget() && side_panel->GetWidget()->IsFullscreen();
   const int window_edge_radius =
       is_fullscreen ? generic_radius : GetPaddedFrameRadius();
-  const bool vertical_tabstrip_on_panel_side =
+  const bool vertical_tabstrip_touches_window_edge =
       browser_view->ShouldDrawVerticalTabStrip() &&
+      (!browser_view->IsZenModeEnabledForLayout() ||
+       browser_view->IsZenModeSideChromePinned()) &&
       (browser_view->IsVerticalTabStripRightAligned() ==
        side_panel->IsRightAligned());
   const int lower_window_edge_radius =
-      !vertical_tabstrip_on_panel_side ? window_edge_radius : generic_radius;
+      vertical_tabstrip_touches_window_edge ? generic_radius
+                                            : window_edge_radius;
 
   return side_panel->IsRightAligned()
              ? gfx::RoundedCornersF(generic_radius, generic_radius,
