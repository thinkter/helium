--- a/chrome/browser/ui/views/side_panel/side_panel_resize_area.cc
+++ b/chrome/browser/ui/views/side_panel/side_panel_resize_area.cc
@@ -20,6 +20,7 @@
 #include "ui/views/controls/focus_ring.h"
 #include "ui/views/controls/image_view.h"
 #include "ui/views/controls/resize_area.h"
+#include "ui/views/controls/separator.h"
 #include "ui/views/layout/flex_layout.h"
 #include "ui/views/view_class_properties.h"
 
@@ -29,7 +30,7 @@ SidePanelResizeHandle::SidePanelResizeHa
     : side_panel_(side_panel) {
   SetProperty(views::kElementIdentifierKey, kSidePanelResizeHandleElementId);
   SetVisible(false);
-  gfx::Size preferred_size(4, 24);
+  gfx::Size preferred_size(2, 24);
   SetPreferredSize(preferred_size);
   SetCanProcessEventsWithinSubtree(false);
 
@@ -142,18 +143,27 @@ void SidePanelResizeArea::OnBlur() {
 
 void SidePanelResizeArea::Layout(PassKey) {
   LayoutSuperclass<ResizeArea>(this);
-  // The side panel resize area should draw on top of its parent's border.
+  // The side panel resize area should fill the margin outside the content
+  // outline without covering the outline itself.
   gfx::Rect local_bounds = parent()->GetLocalBounds();
   gfx::Rect contents_bounds = parent()->GetContentsBounds();
 
   gfx::Rect resize_bounds;
   if ((side_panel_->IsRightAligned() && !base::i18n::IsRTL()) ||
       (!side_panel_->IsRightAligned() && base::i18n::IsRTL())) {
-    resize_bounds = gfx::Rect(local_bounds.x(), local_bounds.y(),
-                              contents_bounds.x(), local_bounds.height());
+    int resize_width = contents_bounds.x() - views::Separator::kThickness;
+    if (resize_width < 0) {
+      resize_width = 0;
+    }
+    resize_bounds = gfx::Rect(local_bounds.x(), local_bounds.y(), resize_width,
+                              local_bounds.height());
   } else {
-    resize_bounds = gfx::Rect(contents_bounds.right(), local_bounds.y(),
-                              local_bounds.right() - contents_bounds.right(),
+    const int resize_x = contents_bounds.right() + views::Separator::kThickness;
+    int resize_width = local_bounds.right() - resize_x;
+    if (resize_width < 0) {
+      resize_width = 0;
+    }
+    resize_bounds = gfx::Rect(resize_x, local_bounds.y(), resize_width,
                               local_bounds.height());
   }
 
--- a/chrome/browser/ui/views/side_panel/side_panel.cc
+++ b/chrome/browser/ui/views/side_panel/side_panel.cc
@@ -29,6 +29,7 @@
 #include "chrome/browser/ui/views/chrome_layout_provider.h"
 #include "chrome/browser/ui/views/frame/browser_view.h"
 #include "chrome/browser/ui/views/frame/themed_background.h"
+#include "chrome/browser/ui/views/helium/frame_corner_radius.h"
 #include "chrome/browser/ui/views/side_panel/side_panel_resize_area.h"
 #include "chrome/common/pref_names.h"
 #include "chrome/grit/generated_resources.h"
@@ -62,31 +63,84 @@
 #include "ui/views/layout/layout_provider.h"
 #include "ui/views/view_class_properties.h"
 #include "ui/views/view_observer.h"
+#include "ui/views/widget/widget.h"
 
 namespace {
 
+constexpr int kContentMargin = 3;
+constexpr int kResizeAreaMargin = 6;
+constexpr int kSidePanelMinimumContentWidth = 200;
+constexpr int kSidePanelDefaultWidthSnapDistance = 15;
+
 constexpr BrowserAnimationGroup kAnimationGroup =
     SidePanelAnimations::kSidePanel;
 
+gfx::RoundedCornersF GetContentRoundedCorners(const BrowserView* browser_view,
+                                              const SidePanel* side_panel) {
+  const int generic_radius = GetGenericFrameRadius();
+  const bool is_fullscreen =
+      side_panel->GetWidget() && side_panel->GetWidget()->IsFullscreen();
+  const int window_edge_radius =
+      is_fullscreen ? generic_radius : GetPaddedFrameRadius();
+  const bool lower_corner_touches_window_edge =
+      !browser_view->ShouldDrawVerticalTabStrip() ||
+      side_panel->IsRightAligned() != base::i18n::IsRTL();
+  const int lower_window_edge_radius =
+      lower_corner_touches_window_edge ? window_edge_radius : generic_radius;
+
+  return side_panel->IsRightAligned()
+             ? gfx::RoundedCornersF(generic_radius, generic_radius,
+                                    lower_window_edge_radius, generic_radius)
+             : gfx::RoundedCornersF(generic_radius, generic_radius,
+                                    generic_radius, lower_window_edge_radius);
+}
+
+void RoundedCornersToSkVectors(const gfx::RoundedCornersF& radii,
+                               SkVector sk_radii[4],
+                               float scale = 1.0f) {
+  const auto get_radius_vector = [scale](float radius) {
+    const float scaled_radius = radius * scale;
+    return SkVector{scaled_radius, scaled_radius};
+  };
+  sk_radii[SkRRect::kUpperLeft_Corner] =
+      get_radius_vector(radii.upper_left());
+  sk_radii[SkRRect::kUpperRight_Corner] =
+      get_radius_vector(radii.upper_right());
+  sk_radii[SkRRect::kLowerRight_Corner] =
+      get_radius_vector(radii.lower_right());
+  sk_radii[SkRRect::kLowerLeft_Corner] =
+      get_radius_vector(radii.lower_left());
+}
+
 // This thickness includes the solid-color background and the inner round-rect
 // border-color stroke. It does not include the outer-color separator.
-int GetBorderThickness() {
-  return 8 + views::Separator::kThickness;
+int GetBorderThickness(int margin) {
+  return margin + views::Separator::kThickness;
 }
 
 // This is how many units of the toolbar are essentially expected to be
 // background.
 constexpr int kOverlapFromToolbar = 4;
 
-// We want the border to visually look like GetBorderThickness() units on all
-// sides except the top. On the top side, background is drawn on top of the
-// top-content separator and some units of background inside the toolbar (or
-// bookmarks bar) itself. Subtract both of those to not get visually-excessive
-// padding.
-gfx::Insets GetBorderInsets() {
-  int border_thickness = GetBorderThickness();
-  return gfx::Insets::TLBR(-kOverlapFromToolbar, border_thickness,
-                           border_thickness, border_thickness);
+bool IsResizeAreaOnLeft(const SidePanel* side_panel) {
+  return (side_panel->IsRightAligned() && !base::i18n::IsRTL()) ||
+         (!side_panel->IsRightAligned() && base::i18n::IsRTL());
+}
+
+// We want the border to visually look like kContentMargin units on all sides
+// except the top and the resize area edge. On the top side, background is drawn
+// on top of the top-content separator and some units of background inside the
+// toolbar (or bookmarks bar) itself. Subtract both of those to not get
+// visually-excessive padding.
+gfx::Insets GetBorderInsets(const SidePanel* side_panel) {
+  const int content_thickness = GetBorderThickness(kContentMargin);
+  const int resize_area_thickness = GetBorderThickness(kResizeAreaMargin);
+  const bool resize_area_on_left = IsResizeAreaOnLeft(side_panel);
+  return gfx::Insets::TLBR(
+      -kOverlapFromToolbar,
+      resize_area_on_left ? resize_area_thickness : content_thickness,
+      content_thickness,
+      resize_area_on_left ? content_thickness : resize_area_thickness);
 }
 
 SidePanel::HorizontalAlignment GetHorizontalAlignment(
@@ -120,16 +174,14 @@ class ContentParentBackground : public v
 
   void Paint(gfx::Canvas* canvas, views::View* view) const override {
     gfx::RoundedCornersF radii = get_rounded_corners_callback_.Run();
-    SkVector sk_radii[4] = {{radii.upper_left(), radii.upper_left()},
-                            {radii.upper_right(), radii.upper_right()},
-                            {radii.lower_right(), radii.lower_right()},
-                            {radii.lower_left(), radii.lower_left()}};
+    SkVector sk_radii[4];
+    RoundedCornersToSkVectors(radii, sk_radii);
     SkRRect rrect;
     rrect.setRectRadii(gfx::RectToSkRect(view->GetLocalBounds()), sk_radii);
     SkPath path = SkPath::RRect(rrect);
     canvas->ClipPath(path, /*do_anti_alias=*/true);
 
-      ThemedBackground::PaintBackground(canvas, view, browser_view_);
+    ThemedBackground::PaintBackground(canvas, view, browser_view_);
   }
 
  private:
@@ -143,8 +195,8 @@ class ContentParentView : public views::
   METADATA_HEADER(ContentParentView, views::View)
 
  public:
-  explicit ContentParentView(BrowserView* browser_view)
-      : browser_view_(browser_view) {
+  ContentParentView(SidePanel* side_panel, BrowserView* browser_view)
+      : side_panel_(side_panel), browser_view_(browser_view) {
     SetUseDefaultFillLayout(true);
     SetProperty(
         views::kFlexBehaviorKey,
@@ -156,6 +208,14 @@ class ContentParentView : public views::
 
   ~ContentParentView() override = default;
 
+  void UpdateRoundedCorners() {
+    const gfx::RoundedCornersF radii = GetRoundedCorners();
+    for (views::View* child : children()) {
+      UpdateChildRoundedCorners(child, radii);
+    }
+    SchedulePaint();
+  }
+
  private:
   void AddedToWidget() override {
     SetBackground(std::make_unique<ContentParentBackground>(
@@ -173,23 +233,7 @@ class ContentParentView : public views::
     // corners initially, set them in OnChildViewAdded, which is called after
     // View::ViewHierarchyChanged.
     // If the child is a WebView or paints to a layer, round its corners.
-    if (views::IsViewClass<views::WebView>(child)) {
-      views::AsViewClass<views::WebView>(child)
-          ->holder()
-          ->SetNativeViewCornerRadii(GetRoundedCorners());
-    }
-    // Try to detect if the child is a views::View wrapper of a WebView. If so,
-    // round its corners.
-    if (child->children().size() == 1 &&
-        views::IsViewClass<views::WebView>(child->children()[0])) {
-      views::AsViewClass<views::WebView>(child->children()[0])
-          ->holder()
-          ->SetNativeViewCornerRadii(GetRoundedCorners());
-    }
-    if (child->layer()) {
-      child->layer()->SetIsFastRoundedCorner(true);
-      child->layer()->SetRoundedCornerRadius(GetRoundedCorners());
-    }
+    UpdateChildRoundedCorners(child, GetRoundedCorners());
   }
 
   void OnWebContentsAttached(views::WebView* web_view) {
@@ -222,14 +266,31 @@ class ContentParentView : public views::
   }
 
   gfx::RoundedCornersF GetRoundedCorners() {
-    ChromeDistanceMetric corner_radius =
-        ChromeDistanceMetric::DISTANCE_SIDE_PANEL_CONTENT_RADIUS;
-    return GetLayoutProvider()
-               ? gfx::RoundedCornersF(
-                     GetLayoutProvider()->GetDistanceMetric(corner_radius))
-               : gfx::RoundedCornersF();
+    return GetContentRoundedCorners(browser_view_, side_panel_);
+  }
+
+  void UpdateChildRoundedCorners(views::View* child,
+                                 const gfx::RoundedCornersF& radii) {
+    if (views::IsViewClass<views::WebView>(child)) {
+      views::AsViewClass<views::WebView>(child)
+          ->holder()
+          ->SetNativeViewCornerRadii(radii);
+    }
+    // Try to detect if the child is a views::View wrapper of a WebView. If so,
+    // round its corners.
+    if (child->children().size() == 1 &&
+        views::IsViewClass<views::WebView>(child->children()[0])) {
+      views::AsViewClass<views::WebView>(child->children()[0])
+          ->holder()
+          ->SetNativeViewCornerRadii(radii);
+    }
+    if (child->layer()) {
+      child->layer()->SetIsFastRoundedCorner(true);
+      child->layer()->SetRoundedCornerRadius(radii);
+    }
   }
 
+  const raw_ptr<SidePanel> side_panel_;
   const raw_ptr<BrowserView> browser_view_;
   base::ScopedObservation<views::View, views::ViewObserver> view_observation_{
       this};
@@ -300,7 +361,7 @@ SidePanel::SidePanel(BrowserView* browse
   // parent view. content_parent_view_ is added first so it exists behind
   // the resize_area_.
   content_parent_view_ =
-      AddChildView(std::make_unique<ContentParentView>(browser_view));
+      AddChildView(std::make_unique<ContentParentView>(this, browser_view));
   content_parent_view_->SetVisible(false);
 
   resize_area_ =
@@ -330,6 +391,7 @@ SidePanel::SidePanel(BrowserView* browse
   // Set the panel width from the preference or use the minimum size as the
   // default.
   SetPanelWidth(GetMinimumSize().width());
+  UpdateBorderInsets();
 
   SetProperty(views::kElementIdentifierKey, kSidePanelElementId);
 }
@@ -360,6 +422,21 @@ void SidePanel::SetBackgroundRadii(const
   background_radii_ = radii;
 }
 
+void SidePanel::UpdateBorderInsets() {
+  gfx::Insets border_insets = GetBorderInsets(this);
+  if (header_view_) {
+    int top_inset = header_view_->height() - border_insets.top();
+    border_insets += gfx::Insets::TLBR(top_inset, 0, 0, 0);
+  }
+
+  SetBorder(views::CreateEmptyBorder(border_insets));
+}
+
+void SidePanel::UpdateRoundedCorners() {
+  static_cast<ContentParentView*>(content_parent_view_.get())
+      ->UpdateRoundedCorners();
+}
+
 void SidePanel::UpdateWidthOnEntryChanged() {
   SidePanelUI* side_panel_ui = SidePanelUI::From(browser_view_->browser());
   if (!side_panel_ui) {
@@ -388,7 +465,7 @@ void SidePanel::UpdateWidthOnEntryChange
     SetPanelWidth(width_from_pref.value());
   } else {
     SetPanelWidth(side_panel_ui->GetCurrentEntryDefaultContentWidth() +
-                  GetBorderInsets().width());
+                  GetBorderInsets(this).width());
   }
 }
 
@@ -399,7 +476,7 @@ bool SidePanel::IsRightAligned() const {
 gfx::Size SidePanel::GetMinimumSize() const {
   const int min_height = 0;
   return gfx::Size(
-      SidePanelEntry::kSidePanelDefaultContentWidth + GetBorderInsets().width(),
+      kSidePanelMinimumContentWidth + GetBorderInsets(this).width(),
       min_height);
 }
 
@@ -456,17 +533,15 @@ void SidePanel::AddHeaderView(std::uniqu
   header_view_->DeprecatedLayoutImmediately();
   // Update the border so that the insets include space for the header to be
   // placed on top of the border area.
-  int top_inset = header_view_->height() - GetBorderInsets().top();
-  SetBorder(views::CreateEmptyBorder(GetBorderInsets() +
-                                     gfx::Insets::TLBR(top_inset, 0, 0, 0)));
+  UpdateBorderInsets();
 }
 
 void SidePanel::RemoveHeaderView() {
-  SetBorder(views::CreateEmptyBorder(GetBorderInsets().set_top(0)));
   if (header_view_) {
     auto header_view = RemoveChildViewT(header_view_);
     header_view_ = nullptr;
   }
+  UpdateBorderInsets();
 }
 
 gfx::Size SidePanel::GetContentSizeUpperBound() const {
@@ -474,11 +549,14 @@ gfx::Size SidePanel::GetContentSizeUpper
   const int side_panel_height =
       height() > 0 ? height() : browser_view_->height();
 
-  return gfx::Size(std::max(0, side_panel_width - GetBorderInsets().width()),
-                   std::max(0, side_panel_height - GetBorderInsets().height()));
+  return gfx::Size(
+      std::max(0, side_panel_width - GetBorderInsets(this).width()),
+      std::max(0, side_panel_height - GetBorderInsets(this).height()));
 }
 
 void SidePanel::OnBoundsChanged(const gfx::Rect& previous_bounds) {
+  UpdateRoundedCorners();
+
   if (previous_bounds.width() != width() && keyboard_resized_) {
     keyboard_resized_ = false;
     AnnounceResize();
@@ -570,6 +648,12 @@ void SidePanel::OnResize(int resize_amou
 
   const int minimum_width = GetMinimumSize().width();
   proposed_width = std::max(proposed_width, minimum_width);
+  const int default_width = SidePanelEntry::kSidePanelDefaultContentWidth +
+                            GetBorderInsets(this).width();
+  if (std::abs(proposed_width - default_width) <
+      kSidePanelDefaultWidthSnapDistance) {
+    proposed_width = default_width;
+  }
 
   if (width() != proposed_width) {
     if (SidePanelUI* side_panel_ui =
@@ -598,7 +682,7 @@ void SidePanel::RecordMetricsIfResized()
       return;
     }
 
-    int side_panel_contents_width = width() - GetBorderInsets().width();
+    int side_panel_contents_width = width() - GetBorderInsets(this).width();
     int browser_window_width = browser_view_->width();
     SidePanelMetrics::RecordSidePanelResizeMetrics(
         id.value(), side_panel_contents_width, browser_window_width);
@@ -756,6 +840,8 @@ void SidePanel::UpdateHorizontalAlignmen
   horizontal_alignment_ =
       GetHorizontalAlignment(browser_view_->GetProfile()->GetPrefs(), entry_id);
 
+  UpdateBorderInsets();
+  UpdateRoundedCorners();
   InvalidateLayout();
 }
 
--- a/chrome/browser/ui/views/side_panel/side_panel.h
+++ b/chrome/browser/ui/views/side_panel/side_panel.h
@@ -137,6 +137,8 @@ class SidePanel : public views::Accessib
 
   bool ShouldShowAnimation() const;
   void AnnounceResize();
+  void UpdateBorderInsets();
+  void UpdateRoundedCorners();
 
   // views::View:
   void OnBoundsChanged(const gfx::Rect& previous_bounds) override;
--- a/chrome/browser/ui/views/side_panel/side_panel_header.cc
+++ b/chrome/browser/ui/views/side_panel/side_panel_header.cc
@@ -23,7 +23,7 @@
 
 namespace {
 // The minimum cross axis size should the expected height of the header.
-constexpr int kDefaultSidePanelHeaderHeight = 40;
+constexpr int kDefaultSidePanelHeaderHeight = 32;
 }  // namespace
 
 SidePanelHeader::SidePanelHeader(
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -193,10 +193,9 @@ BrowserViewTabbedLayoutImpl::CalculateSe
   switch (layout_data_->window_state) {
     case WindowState::kNormal:
     case WindowState::kMaximized: {
-      // In content-height mode, split view outline serves as shadow box.
-      info.shadow_box =
-          has_side_panel && (has_infobar || !is_split_view || !is_force_to_top);
-      const bool suppress_separator = info.shadow_box && is_force_to_top;
+      // Side panel shadow overlay is disabled in Helium.
+      info.shadow_box = false;
+      const bool suppress_separator = false;
       info.multi_contents_separator = !suppress_separator && !has_infobar;
       info.top_container_separator = !suppress_separator && has_infobar;
       info.side_panel_top_padding = !is_force_to_top;
