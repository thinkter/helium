--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -2296,6 +2296,9 @@
     <message name="IDS_SETTINGS_SEARCH_ENGINES_ADD_SITE_SEARCH" desc="Title for a dialog that allows adding a new site search engine.">
       Add site search
     </message>
+    <message name="IDS_SETTINGS_SEARCH_ENGINES_ADD_SEARCH_ENGINE" desc="Title for a dialog that allows adding a new search engine.">
+      Add search engine
+    </message>
     <message name="IDS_SETTINGS_SEARCH_ENGINES_EDIT_SEARCH_ENGINE" desc="Title for a dialog that allows editing an existing search engine.">
       Edit search engine
     </message>
--- a/components/components_strings.grd
+++ b/components/components_strings.grd
@@ -392,6 +392,9 @@
       <message name="IDS_ADD" desc="Used for Add on buttons">
         Add
       </message>
+      <message name="IDS_ADD_AS_DEFAULT" desc="Used on a button to add information and then set it as default.">
+        Add as default
+      </message>
       <message name="IDS_MOVE" desc="Used for Move on buttons" formatter_data="android_java">
         Move
       </message>
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -185,6 +185,7 @@ namespace {
 void AddCommonStrings(content::WebUIDataSource* html_source, Profile* profile) {
   static constexpr webui::LocalizedString kLocalizedStrings[] = {
       {"add", IDS_ADD},
+      {"addAsDefault", IDS_ADD_AS_DEFAULT},
       {"advancedPageTitle", IDS_SETTINGS_ADVANCED},
       {"back", IDS_ACCNAME_BACK},
       {"basicPageTitle", IDS_SETTINGS_BASIC},
@@ -3200,6 +3201,8 @@ void AddSearchEnginesStrings(content::We
        IDS_SETTINGS_SEARCH_ENGINES_PAGE_EXPLANATION},
       {"searchEnginesAddSiteSearch",
        IDS_SETTINGS_SEARCH_ENGINES_ADD_SITE_SEARCH},
+      {"searchEnginesAddSearchEngine",
+       IDS_SETTINGS_SEARCH_ENGINES_ADD_SEARCH_ENGINE},
       {"searchEnginesEditSearchEngine",
        IDS_SETTINGS_SEARCH_ENGINES_EDIT_SEARCH_ENGINE},
       {"searchEnginesViewSearchEngine",
--- a/chrome/browser/resources/settings/search_page/search_engine_edit_dialog.ts
+++ b/chrome/browser/resources/settings/search_page/search_engine_edit_dialog.ts
@@ -24,7 +24,7 @@ import {loadTimeData} from '../i18n_setu
 import {getCss} from './search_engine_edit_dialog.css.js';
 import {getHtml} from './search_engine_edit_dialog.html.js';
 import type {CategorizedTemplateUrls, SearchEngine, SearchEnginesBrowserProxy, SearchEnginesInfo} from './search_engines_browser_proxy.js';
-import {SearchEnginesBrowserProxyImpl} from './search_engines_browser_proxy.js';
+import {ChoiceMadeLocation, SearchEnginesBrowserProxyImpl} from './search_engines_browser_proxy.js';
 
 // The `id` to use when a new search engine is added.  See
 // `kInvalidTemplateURLID`.
@@ -66,6 +66,7 @@ export class SettingsSearchEngineEditDia
        * should be added.
        */
       model: {type: Object},
+      isNewPrimary: {type: Boolean},
       searchEngine_: {type: String},
       keyword_: {type: String},
       queryUrl_: {type: String},
@@ -79,6 +80,7 @@ export class SettingsSearchEngineEditDia
   }
 
   accessor model: SearchEngine|null = null;
+  accessor isNewPrimary: boolean = false;
   protected accessor searchEngine_: string = '';
   protected accessor keyword_: string = '';
   protected accessor queryUrl_: string = '';
@@ -108,7 +110,8 @@ export class SettingsSearchEngineEditDia
   override willUpdate(changedProperties: PropertyValues<this>) {
     super.willUpdate(changedProperties);
 
-    if (changedProperties.has('model')) {
+    if (changedProperties.has('model') ||
+        changedProperties.has('isNewPrimary')) {
       if (this.model) {
         this.readonly_ = this.model.isManaged && !this.model.canBeEdited;
         if (this.model.isPrepopulated || this.model.default) {
@@ -131,9 +134,11 @@ export class SettingsSearchEngineEditDia
         this.queryUrl_ = this.model.url;
         this.suggestionsUrl_ = this.model.suggestionsUrl;
       } else {
-        this.dialogTitle_ =
-            loadTimeData.getString('searchEnginesAddSiteSearch');
-        this.actionButtonText_ = loadTimeData.getString('add');
+        this.dialogTitle_ = loadTimeData.getString(
+            this.isNewPrimary ? 'searchEnginesAddSearchEngine' :
+                                'searchEnginesAddSiteSearch');
+        this.actionButtonText_ =
+            loadTimeData.getString(this.isNewPrimary ? 'addAsDefault' : 'add');
         this.readonly_ = false;
         this.showPolicySubtitle_ = false;
         this.searchEngine_ = '';
@@ -261,7 +266,23 @@ export class SettingsSearchEngineEditDia
     this.browserProxy_.searchEngineEditCompleted(
         this.searchEngine_, this.keyword_, this.queryUrl_,
         this.suggestionsUrl_);
-    this.$.dialog.close();
+    const done = () => this.$.dialog.close();
+    if (!this.isNewPrimary) {
+      return done();
+    }
+
+    this.browserProxy_.getSearchEnginesList()
+      .then(engines => {
+        const engine = engines.actives.find(e => e.keyword === this.keyword_);
+
+        if (!engine || !engine.canBeDefault) {
+          return;
+        }
+
+        this.browserProxy_.setDefaultSearchEngine(
+            engine.id, ChoiceMadeLocation.SEARCH_ENGINE_SETTINGS, null);
+      })
+      .finally(done);
   }
 
   private validateElement_(inputElement: CrInputElement) {
@@ -281,7 +302,8 @@ export class SettingsSearchEngineEditDia
     }
 
     this.browserProxy_
-        .validateSearchEngineInput(inputElement.id, inputElement.value)
+        .validateSearchEngineInput(
+            inputElement.id, inputElement.value, this.isNewPrimary)
         .then(isValid => {
           inputElement.invalid = !isValid;
           this.updateActionButtonState_();
--- a/chrome/browser/resources/settings/search_page/search_engines_page.html.ts
+++ b/chrome/browser/resources/settings/search_page/search_engines_page.html.ts
@@ -53,6 +53,11 @@ export function getHtml(this: SettingsSe
         $i18n{searchEnginesSearchEnginesExplanation}
       </div>
     </div>
+    <cr-button class="secondary-button header-aligned-button"
+        aria-label="$i18n{searchEnginesAddButtonAriaLabel}"
+        @click="${this.onAddPrimarySearchEngineClick_}" id="addPrimarySearchEngine">
+      $i18n{add}
+    </cr-button>
   </div>
   <settings-search-engines-list ?hidden="${!this.matchingDefaultEngines_.length}"
       .engines="${this.matchingDefaultEngines_}" show-shortcut>
@@ -65,6 +70,7 @@ export function getHtml(this: SettingsSe
 
   ${this.showEditDialog_ ? html`
     <settings-search-engine-edit-dialog .model="${this.dialogModel_}"
+        .isNewPrimary="${this.isNewPrimary_}"
         @close="${this.onEditDialogClose_}">
     </settings-search-engine-edit-dialog>
   ` : ''}
--- a/chrome/browser/resources/settings/search_page/search_engines_page.ts
+++ b/chrome/browser/resources/settings/search_page/search_engines_page.ts
@@ -82,6 +82,7 @@ export class SettingsSearchEnginesPageEl
 
       dialogModel_: {type: Object},
       dialogAnchorElement_: {type: Object},
+      isNewPrimary_: {type: Boolean},
       showEditDialog_: {type: Boolean},
       showDeleteConfirmationDialog_: {type: Boolean},
     };
@@ -99,6 +100,7 @@ export class SettingsSearchEnginesPageEl
   protected accessor matchingExtensions_: SearchEngine[] = [];
   protected accessor dialogModel_: SearchEngine|null = null;
   protected accessor dialogAnchorElement_: HTMLElement|null = null;
+  protected accessor isNewPrimary_: boolean = false;
   protected accessor showEditDialog_: boolean = false;
   protected accessor showDeleteConfirmationDialog_: boolean = false;
 
@@ -186,9 +188,11 @@ export class SettingsSearchEnginesPageEl
   }
 
   private openEditDialog_(
-      searchEngine: SearchEngine|null, anchorElement: HTMLElement) {
+      searchEngine: SearchEngine|null, anchorElement: HTMLElement,
+      primary = false) {
     this.dialogModel_ = searchEngine;
     this.dialogAnchorElement_ = anchorElement;
+    this.isNewPrimary_ = primary;
     this.showEditDialog_ = true;
   }
 
@@ -245,6 +249,12 @@ export class SettingsSearchEnginesPageEl
     this.extensions = searchEnginesInfo.extensions;
   }
 
+  protected onAddPrimarySearchEngineClick_(e: Event) {
+    e.preventDefault();
+    this.openEditDialog_(
+        null, this.shadowRoot.querySelector('#addPrimarySearchEngine')!, true);
+  }
+
   protected onAddSearchEngineClick_(e: Event) {
     e.preventDefault();
     this.browserProxy_.recordSearchEnginesPageHistogram(
--- a/components/search_engines/util.cc
+++ b/components/search_engines/util.cc
@@ -331,7 +331,8 @@ std::string GetFixedUpSearchEngineUrl(
 
 bool IsSearchEngineURLValidToUse(const std::string& url_input,
                                  const TemplateURLService* service,
-                                 const TemplateURL* existing_url) {
+                                 const TemplateURL* existing_url,
+                                 bool is_new_primary) {
   std::string url =
       GetFixedUpSearchEngineUrl(url_input, service->search_terms_data());
   if (url.empty()) {
@@ -352,8 +353,12 @@ bool IsSearchEngineURLValidToUse(const s
 
   // If this is going to be the default search engine, it must support
   // replacement.
+  is_new_primary =
+      is_new_primary ||
+      (existing_url && existing_url == service->GetDefaultSearchProvider());
+
   if (!template_ref.SupportsReplacement(service->search_terms_data()) &&
-      existing_url && existing_url == service->GetDefaultSearchProvider()) {
+      is_new_primary) {
     return false;
   }
 
--- a/chrome/browser/ui/search_engines/edit_search_engine_controller.cc
+++ b/chrome/browser/ui/search_engines/edit_search_engine_controller.cc
@@ -32,15 +32,17 @@ bool EditSearchEngineController::IsTitle
   return IsSearchEngineNameValidToUse(title_input);
 }
 
-bool EditSearchEngineController::IsURLValid(
-    const std::string& url_input) const {
+bool EditSearchEngineController::IsURLValid(const std::string& url_input,
+                                            const bool is_new_primary) const {
   TemplateURLService* service =
       TemplateURLServiceFactory::GetForProfile(profile_);
-  return IsSearchEngineURLValidToUse(url_input, service, template_url_);
+  return IsSearchEngineURLValidToUse(url_input, service, template_url_,
+                                     is_new_primary);
 }
 
 bool EditSearchEngineController::IsSuggestionsURLValid(
-    const std::string& suggestions_url_input) const {
+    const std::string& suggestions_url_input,
+    const bool is_new_primary) const {
   TemplateURLService* template_url_service =
       TemplateURLServiceFactory::GetForProfile(profile_);
   std::string suggestions_url =
@@ -49,7 +51,7 @@ bool EditSearchEngineController::IsSugge
   if (suggestions_url.empty())
     return true;
 
-  return IsURLValid(suggestions_url);
+  return IsURLValid(suggestions_url, is_new_primary);
 }
 
 bool EditSearchEngineController::IsKeywordValid(
--- a/chrome/browser/resources/settings/search_page/search_engines_browser_proxy.ts
+++ b/chrome/browser/resources/settings/search_page/search_engines_browser_proxy.ts
@@ -131,8 +131,9 @@ export interface SearchEnginesBrowserPro
 
   getSaveGuestChoice(): Promise<boolean|null>;
 
-  validateSearchEngineInput(fieldName: string, fieldValue: string):
-      Promise<boolean>;
+  validateSearchEngineInput(
+      fieldName: string, fieldValue: string,
+      isNewPrimary: boolean): Promise<boolean>;
 
   /**
    * Helper function that calls recordHistogram for the
@@ -193,9 +194,10 @@ export class SearchEnginesBrowserProxyIm
     return sendWithPromise<boolean|null>('getSaveGuestChoice');
   }
 
-  validateSearchEngineInput(fieldName: string, fieldValue: string) {
+  validateSearchEngineInput(
+      fieldName: string, fieldValue: string, isNewPrimary: boolean) {
     return sendWithPromise<boolean>(
-        'validateSearchEngineInput', fieldName, fieldValue);
+        'validateSearchEngineInput', fieldName, fieldValue, isNewPrimary);
   }
 
   recordSearchEnginesPageHistogram(interaction: SearchEnginesInteractions) {
--- a/chrome/browser/ui/webui/settings/search_engines_handler.cc
+++ b/chrome/browser/ui/webui/settings/search_engines_handler.cc
@@ -506,17 +506,19 @@ void SearchEnginesHandler::OnEditedKeywo
 
 void SearchEnginesHandler::HandleValidateSearchEngineInput(
     const base::ListValue& args) {
-  CHECK_EQ(3U, args.size());
+  CHECK_EQ(4U, args.size());
 
   const base::Value& callback_id = args[0];
   const std::string& field_name = args[1].GetString();
   const std::string& field_value = args[2].GetString();
+  const bool is_new_primary = args[3].GetBool();
   ResolveJavascriptCallback(
-      callback_id, base::Value(CheckFieldValidity(field_name, field_value)));
+      callback_id, base::Value(CheckFieldValidity(field_name, field_value, is_new_primary)));
 }
 
 bool SearchEnginesHandler::CheckFieldValidity(const std::string& field_name,
-                                              const std::string& field_value) {
+                                              const std::string& field_value,
+                                              const bool is_new_primary) {
   if (!edit_controller_.get()) {
     return false;
   }
@@ -527,9 +529,9 @@ bool SearchEnginesHandler::CheckFieldVal
   } else if (field_name.compare(kKeywordField) == 0) {
     is_valid = edit_controller_->IsKeywordValid(base::UTF8ToUTF16(field_value));
   } else if (field_name.compare(kQueryUrlField) == 0) {
-    is_valid = edit_controller_->IsURLValid(field_value);
+    is_valid = edit_controller_->IsURLValid(field_value, is_new_primary);
   } else if (field_name.compare(kSuggestionsUrlField) == 0) {
-    is_valid = edit_controller_->IsSuggestionsURLValid(field_value);
+    is_valid = edit_controller_->IsSuggestionsURLValid(field_value, is_new_primary);
   } else {
     NOTREACHED();
   }
--- a/chrome/browser/ui/search_engines/edit_search_engine_controller.h
+++ b/chrome/browser/ui/search_engines/edit_search_engine_controller.h
@@ -52,9 +52,9 @@ class EditSearchEngineController {
   // URL. The URL is valid if it contains no search terms and is a valid
   // url, or if it contains a search term and replacing that search term with a
   // character results in a valid url.
-  bool IsURLValid(const std::string& url_input) const;
+  bool IsURLValid(const std::string& url_input, bool is_new_primary = false) const;
 
-  bool IsSuggestionsURLValid(const std::string& suggestions_url_input) const;
+  bool IsSuggestionsURLValid(const std::string& suggestions_url_input, bool is_new_primary = false) const;
 
   // Returns true if the value of |keyword_input| represents a valid keyword.
   // The keyword is valid if it is non-empty and does not conflict with an
--- a/chrome/browser/ui/webui/settings/search_engines_handler.h
+++ b/chrome/browser/ui/webui/settings/search_engines_handler.h
@@ -87,7 +87,8 @@ class SearchEnginesHandler : public Sett
   // Checks whether the given user input field (searchEngine, keyword, queryUrl,
   // suggestionsUrl) is populated with a valid value.
   bool CheckFieldValidity(const std::string& field_name,
-                          const std::string& field_value);
+                          const std::string& field_value,
+                          const bool is_new_primary = false);
 
   // Called when an edit is canceled.
   // Called from WebUI.
--- a/components/search_engines/util.h
+++ b/components/search_engines/util.h
@@ -269,7 +269,8 @@ bool IsSearchEngineKeywordValidToUse(con
 // new one.
 bool IsSearchEngineURLValidToUse(const std::string& url_input,
                                  const TemplateURLService* service,
-                                 const TemplateURL* existing_url);
+                                 const TemplateURL* existing_url,
+                                 const bool is_new_primary = false);
 
 // Fixes up and returns the URL. The returned URL is suitable for use by
 // TemplateURL.
