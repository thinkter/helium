--- a/chrome/common/pref_names.h
+++ b/chrome/common/pref_names.h
@@ -608,6 +608,11 @@ inline constexpr char kNewTabNextToActiv
 // when cycling through tabs with Ctrl+Tab.
 inline constexpr char kTabCyclingMRU[] = "helium.browser.mru_tab_cycling";
 
+// A boolean pref set to true if Shift+right-click should bypass page context
+// menu handlers and show the browser context menu.
+inline constexpr char kShiftRightClickContextMenu[] =
+    "helium.browser.shift_right_click_context_menu";
+
 // A boolean pref set to true if a Home button to open the Home pages should be
 // visible on the toolbar.
 inline constexpr char kShowHomeButton[] = "browser.show_home_button";
--- a/chrome/browser/ui/browser_ui_prefs.cc
+++ b/chrome/browser/ui/browser_ui_prefs.cc
@@ -129,6 +129,8 @@ void RegisterBrowserUserPrefs(user_prefs
 
   registry->RegisterBooleanPref(prefs::kTabCyclingMRU, false);
 
+  registry->RegisterBooleanPref(prefs::kShiftRightClickContextMenu, true);
+
   registry->RegisterBooleanPref(prefs::kHomePageIsNewTabPage, true,
                                 pref_registration_flags);
   registry->RegisterBooleanPref(prefs::kShowHomeButton, false,
--- a/third_party/blink/renderer/core/frame/settings.json5
+++ b/third_party/blink/renderer/core/frame/settings.json5
@@ -806,6 +806,11 @@
       initial: false,
     },
 
+    {
+      name: "shiftRightClickContextMenuEnabled",
+      initial: true,
+    },
+
     // If true, context menu will be shown on any long press event.
     // Used on Android to prevent a context menu from being shown in certain situations
     // (i.e. long pressing an empty div)
--- a/third_party/blink/renderer/core/input/event_handler.cc
+++ b/third_party/blink/renderer/core/input/event_handler.cc
@@ -90,6 +90,7 @@
 #include "third_party/blink/renderer/core/loader/resource/image_resource_content.h"
 #include "third_party/blink/renderer/core/page/autoscroll_controller.h"
 #include "third_party/blink/renderer/core/page/chrome_client.h"
+#include "third_party/blink/renderer/core/page/context_menu_controller.h"
 #include "third_party/blink/renderer/core/page/drag_state.h"
 #include "third_party/blink/renderer/core/page/frame_tree.h"
 #include "third_party/blink/renderer/core/page/page.h"
@@ -2206,6 +2207,17 @@ WebInputEventResult EventHandler::SendCo
   // |SelectionController::sendContextMenuEvent()|.
   document.UpdateStyleAndLayout(DocumentUpdateReason::kContextMenu);
 
+  if (frame_->GetSettings()->GetShiftRightClickContextMenuEnabled() &&
+      event.button == WebPointerProperties::Button::kRight &&
+      (event.GetModifiers() & WebInputEvent::Modifiers::kShiftKey)) {
+    Page* page = document.GetPage();
+    if (page && page->GetContextMenuController().ShowContextMenuForUserInput(
+                    frame_, position_in_contents, event.GetMenuSourceType())) {
+      return WebInputEventResult::kHandledSystem;
+    }
+    return WebInputEventResult::kNotHandled;
+  }
+
   Element* target_element = override_target_element
                                 ? override_target_element
                                 : mev.InnerPossiblyPseudoElement();
--- a/third_party/blink/renderer/core/page/context_menu_controller.cc
+++ b/third_party/blink/renderer/core/page/context_menu_controller.cc
@@ -460,6 +460,13 @@ bool ContextMenuController::ShowContextM
   return ShowContextMenu(frame, point, source_type, nullptr);
 }
 
+bool ContextMenuController::ShowContextMenuForUserInput(
+    LocalFrame* frame,
+    const PhysicalOffset& point,
+    ui::mojom::blink::MenuSourceType source_type) {
+  return ShowContextMenu(frame, point, source_type);
+}
+
 bool ContextMenuController::ShowContextMenu(
     LocalFrame* frame,
     const PhysicalOffset& point,
--- a/third_party/blink/renderer/core/page/context_menu_controller.h
+++ b/third_party/blink/renderer/core/page/context_menu_controller.h
@@ -63,6 +63,10 @@ class CORE_EXPORT ContextMenuController
                               float y,
                               ContextMenuProvider*);
 
+  bool ShowContextMenuForUserInput(LocalFrame*,
+                                   const PhysicalOffset&,
+                                   ui::mojom::blink::MenuSourceType);
+
   void CustomContextMenuItemSelected(unsigned action);
 
   Node* ContextMenuImageNodeForFrame(LocalFrame*);
--- a/third_party/blink/public/web/web_settings.h
+++ b/third_party/blink/public/web/web_settings.h
@@ -201,6 +201,7 @@ class WebSettings {
                                   UScriptCode = USCRIPT_COMMON) = 0;
   virtual void SetShouldPrintBackgrounds(bool) = 0;
   virtual void SetShouldClearDocumentBackground(bool) = 0;
+  virtual void SetShiftRightClickContextMenuEnabled(bool) = 0;
   virtual void SetShowContextMenuOnMouseUp(bool) = 0;
   virtual void SetShrinksViewportContentToFit(bool) = 0;
   virtual void SetSmartInsertDeleteEnabled(bool) = 0;
--- a/third_party/blink/renderer/core/exported/web_settings_impl.cc
+++ b/third_party/blink/renderer/core/exported/web_settings_impl.cc
@@ -476,6 +476,10 @@ void WebSettingsImpl::SetSmoothScrollFor
   settings_->SetSmoothScrollForFindEnabled(enabled);
 }
 
+void WebSettingsImpl::SetShiftRightClickContextMenuEnabled(bool enabled) {
+  settings_->SetShiftRightClickContextMenuEnabled(enabled);
+}
+
 void WebSettingsImpl::SetShowContextMenuOnMouseUp(bool enabled) {
   settings_->SetShowContextMenuOnMouseUp(enabled);
 }
--- a/third_party/blink/renderer/core/exported/web_settings_impl.h
+++ b/third_party/blink/renderer/core/exported/web_settings_impl.h
@@ -155,6 +155,7 @@ class CORE_EXPORT WebSettingsImpl final
                           UScriptCode = USCRIPT_COMMON) override;
   void SetShouldPrintBackgrounds(bool) override;
   void SetShouldClearDocumentBackground(bool) override;
+  void SetShiftRightClickContextMenuEnabled(bool) override;
   void SetShowContextMenuOnMouseUp(bool) override;
   void SetShrinksViewportContentToFit(bool) override;
   void SetSmartInsertDeleteEnabled(bool) override;
--- a/third_party/blink/common/web_preferences/web_preferences_mojom_traits.cc
+++ b/third_party/blink/common/web_preferences/web_preferences_mojom_traits.cc
@@ -56,6 +56,8 @@ bool StructTraits<blink::mojom::WebPrefe
   out->minimum_font_size = data.minimum_font_size();
   out->minimum_logical_font_size = data.minimum_logical_font_size();
   out->context_menu_on_mouse_up = data.context_menu_on_mouse_up();
+  out->shift_right_click_context_menu_enabled =
+      data.shift_right_click_context_menu_enabled();
   out->javascript_enabled = data.javascript_enabled();
   out->web_security_enabled = data.web_security_enabled();
   out->loads_images_automatically = data.loads_images_automatically();
--- a/chrome/browser/chrome_content_browser_client.cc
+++ b/chrome/browser/chrome_content_browser_client.cc
@@ -4641,6 +4641,8 @@ void ChromeContentBrowserClient::Overrid
   web_prefs->javascript_can_access_clipboard =
       prefs->GetBoolean(prefs::kWebKitJavascriptCanAccessClipboard);
   web_prefs->tabs_to_links = prefs->GetBoolean(prefs::kWebkitTabsToLinks);
+  web_prefs->shift_right_click_context_menu_enabled =
+      prefs->GetBoolean(prefs::kShiftRightClickContextMenu);
 
   if (!prefs->GetBoolean(prefs::kWebKitJavascriptEnabled)) {
     web_prefs->javascript_enabled = false;
--- a/chrome/browser/ui/prefs/pref_watcher.cc
+++ b/chrome/browser/ui/prefs/pref_watcher.cc
@@ -54,6 +54,7 @@ const char* const kWebPrefsToObserve[] =
     prefs::kDisable3DAPIs,
     prefs::kEnableHyperlinkAuditing,
     prefs::kSubresourceFilterHighlightAds,
+    prefs::kShiftRightClickContextMenu,
     prefs::kWebKitAllowRunningInsecureContent,
     prefs::kWebKitDefaultFixedFontSize,
     prefs::kWebKitDefaultFontSize,
--- a/third_party/blink/public/common/web_preferences/web_preferences.h
+++ b/third_party/blink/public/common/web_preferences/web_preferences.h
@@ -62,6 +62,7 @@ struct BLINK_COMMON_EXPORT WebPreference
   int minimum_logical_font_size = 6;
   std::string default_encoding = "ISO-8859-1";
   bool context_menu_on_mouse_up = BUILDFLAG(IS_WIN);
+  bool shift_right_click_context_menu_enabled = true;
   bool javascript_enabled = true;
   bool web_security_enabled = true;
   bool loads_images_automatically = true;
--- a/third_party/blink/public/common/web_preferences/web_preferences_mojom_traits.h
+++ b/third_party/blink/public/common/web_preferences/web_preferences_mojom_traits.h
@@ -83,6 +83,11 @@ struct BLINK_COMMON_EXPORT StructTraits<
     return r.context_menu_on_mouse_up;
   }
 
+  static bool shift_right_click_context_menu_enabled(
+      const blink::web_pref::WebPreferences& r) {
+    return r.shift_right_click_context_menu_enabled;
+  }
+
   static bool javascript_enabled(const blink::web_pref::WebPreferences& r) {
     return r.javascript_enabled;
   }
--- a/third_party/blink/public/mojom/webpreferences/web_preferences.mojom
+++ b/third_party/blink/public/mojom/webpreferences/web_preferences.mojom
@@ -128,6 +128,7 @@ struct WebPreferences {
   int32 minimum_logical_font_size;
   string default_encoding;
   bool context_menu_on_mouse_up;
+  bool shift_right_click_context_menu_enabled;
   bool javascript_enabled;
   bool web_security_enabled;
   bool loads_images_automatically;
--- a/third_party/blink/renderer/core/exported/web_view_impl.cc
+++ b/third_party/blink/renderer/core/exported/web_view_impl.cc
@@ -1886,6 +1886,8 @@ void WebView::ApplyWebPreferences(const
       prefs.main_frame_resizes_are_orientation_changes);
 
   settings->SetShowContextMenuOnMouseUp(prefs.context_menu_on_mouse_up);
+  settings->SetShiftRightClickContextMenuEnabled(
+      prefs.shift_right_click_context_menu_enabled);
   settings->SetAlwaysShowContextMenuOnTouch(
       prefs.always_show_context_menu_on_touch);
   settings->SetSmoothScrollForFindEnabled(prefs.smooth_scroll_for_find_enabled);
--- a/chrome/app/settings_strings.grdp
+++ b/chrome/app/settings_strings.grdp
@@ -265,6 +265,9 @@
     <message name="IDS_SETTINGS_CYCLE_TABS_IN_MRU_ORDER" desc="Label for the checkbox which enables or disables cycling through tabs in MRU order when using Ctrl+Tab.">
       Cycle through tabs in most recently used order with Ctrl+Tab
     </message>
+    <message name="IDS_SETTINGS_SHIFT_RIGHT_CLICK_CONTEXT_MENU" desc="Label for the checkbox which makes Shift+right click show the browser context menu even when a page blocks it.">
+      Force context menu with Shift+Right Click
+    </message>
     <message name="IDS_SETTINGS_SHOW_HOME_BUTTON" desc="Label for the checkbox which enables or disables showing the home button in the toolbar.">
       Show home button
     </message>
--- a/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ b/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -301,6 +301,8 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kTabCyclingMRU] =
       settings_api::PrefType::kBoolean;
+  (*s_allowlist)[::prefs::kShiftRightClickContextMenu] =
+      settings_api::PrefType::kBoolean;
 
   (*s_allowlist)[::prefs::kShowHomeButton] = settings_api::PrefType::kBoolean;
   (*s_allowlist)[contextual_search::kDriveConsentState] =
--- a/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ b/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -594,6 +594,8 @@ void AddAppearanceStrings(content::WebUI
       {"systemMode", IDS_NTP_CUSTOMIZE_CHROME_COLOR_SCHEME_MODE_SYSTEM_LABEL},
       {"openNewTabNextToActive", IDS_SETTINGS_NEW_TAB_NEXT_TO_ACTIVE_BUTTON},
       {"cycleTabsInMRUOrder", IDS_SETTINGS_CYCLE_TABS_IN_MRU_ORDER},
+      {"shiftRightClickContextMenu",
+       IDS_SETTINGS_SHIFT_RIGHT_CLICK_CONTEXT_MENU},
       {"showHomeButton", IDS_SETTINGS_SHOW_HOME_BUTTON},
       {"showBookmarksBar", IDS_SETTINGS_SHOW_BOOKMARKS_BAR},
       {"bookmarksBar", IDS_SETTINGS_BOOKMARKS_BAR},
--- a/chrome/browser/resources/settings/appearance_page/appearance_behavior_page.html
+++ b/chrome/browser/resources/settings/appearance_page/appearance_behavior_page.html
@@ -11,6 +11,11 @@
       label="$i18n{cycleTabsInMRUOrder}">
   </settings-toggle-button>
 
+  <settings-toggle-button class="hr" id="shiftRightClickContextMenu"
+      pref-key="helium.browser.shift_right_click_context_menu"
+      label="$i18n{shiftRightClickContextMenu}">
+  </settings-toggle-button>
+
   <settings-toggle-button class="hr" id="copyPageUrlShortcut"
       pref-key="helium.settings.a11y.copy_page_url_shortcut"
       label="$i18n{copyPageUrlShortcut}">
